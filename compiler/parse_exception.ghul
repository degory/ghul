namespace Compiler is
    use System;

    class PARSE_EXCEPTION: Exception is
        init(message: String) is
            super.init(message);
        si

        init() is
            super.init();
        si
    si
si