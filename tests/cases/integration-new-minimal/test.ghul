namespace Test is
    class Main is
        init() static is
            @IL.entrypoint()

            let x = new Thing();
        si
    si

    class Thing is
        init() is
            System.Console.write_line("I am a new Thing");
        si
    si  
si

@IL.stub()
namespace System is
    @IL.name("[mscorlib]System.Console")
    class Console is
        @IL.name("WriteLine")
        write_line(text: String) static;
    si
si