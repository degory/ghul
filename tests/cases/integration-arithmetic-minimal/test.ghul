namespace Test is
    class Main is
        init() static is
            @IL.entrypoint()

            let x = 123;
            let y = 456;

            let z = x + y;

            let o = z.box();

            System.Console.write_line(z.box().toString());
        si
    si
si

@IL.stub()
namespace System is
    @IL.name("[mscorlib]System.Console")
    class Console is
        @IL.name("WriteLine")
        write_line(text: String) static;
    si
si