namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let thing = new Thing();

            let function = thing.create_test_function();

            thing.call_test_function(function);
        si
    si

    class Thing is
        init() is
        si

        create_test_function() -> (int,int) -> int is
            /*
            let result = (i: int, j: int) -> int is
                System.Console.write_line("test function: " + i + "," + j);
                return i + j;
            si;
            */
            // return result;
        si

        call_test_function(function: (int, int) -> int) is
            let result = function(111, 222);

            System.Console.write_line("result is: " + result);
        si        
    si
si

@IL.stub()
namespace System is
    @IL.name("[mscorlib]System.Console")
    class Console is
        @IL.name("WriteLine")
        write_line(text: String) static;
    si
si