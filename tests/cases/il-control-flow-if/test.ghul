namespace Test is
    use System;

    class Main is
        entry() static is
            @IL.entrypoint()

            let a = 10;
            let b = 20;

            let result: int;

            @IL.output("il.out")
            if a <= b then
                result = 1;
            else
                result = 2;
            fi            

            return;
        si
    si
si
