namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let x = "xx";
            let y = "yy";
            let xx = "x" + "x";

            System.Console.write_line("expect true: x == x: " + (x == x));
            System.Console.write_line("expect false: x != x: " + (x != x));

            System.Console.write_line("expect true: xx == xx: " + (xx == xx));
            System.Console.write_line("expect false: xx != xx: " + (xx != xx));

            System.Console.write_line("expect false: x == y: " + (x == y));
            System.Console.write_line("expect true: x != y: " + (x != y));

            System.Console.write_line("expect false: x == xx: " + (x == xx));
            System.Console.write_line("expect true: x != xx: " + (x != xx));

            return;
        si
    si
si

@IL.stub()
namespace System is
    @IL.name("[mscorlib]System.Console")
    class Console is
        @IL.name("WriteLine")
        write_line(text: String) static;
    si
si