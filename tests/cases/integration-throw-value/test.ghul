namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            // FIXME: cannot test this as runtime output is not consistent from one run to the next
            throw 123456;

            System.Console.write_line("should not reach here");
        si
    si
si