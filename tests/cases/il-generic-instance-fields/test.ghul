namespace Test is
    class TEST[T,U] is
        _t: T;
        _u: U;

        init(t: T, u: U) is
            @IL.output("il.out")
            _t = t;
            @IL.output("il.out")
            _u = u;
        si

        get_t() -> T is
            @IL.output("il.out")
            return _t;
        si

        get_u() -> U is
            @IL.output("il.out")
            return _u;            
        si
    si
si