namespace Test.SingleElementTuple is
    use System; 
    use Generic;

    class Test is
        test_tuple(t: (int));
        test_int(i: int);

        test(t: (int));
        test(i: int);

        test() is
            // FIXME: is there any benfit in attempting to support single element tuples? Do they provide any value?

            test_tuple(((123+456)));
            test_tuple((123+456));
            test_tuple((123));

            test_int(((123+456)));
            test_int((123+456));
            test_int((123));
            test_int(123);

            test(((123+456)));
            test((123+456));
            test((123));
            test(((123+456)));
            test((123+456));
            test((123));
            test(123);            
        si
    si
si