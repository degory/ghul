namespace Test.GenericSelf is
    use System;
    use Ghul;

    use Generic;

    class Test[T] is
        test_iterator_self() is
            let a = [self];
            let i: Iterator[Test[T]] = [self].iterator;
        si
    si
si
