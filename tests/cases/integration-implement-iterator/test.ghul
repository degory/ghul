namespace Test is
    use Collections;
    
    class Main is
        entry() static is
            @IL.entrypoint()

            let things = new Things(20);

            for thing in things do
                System.Console.write_line("have thing: " + thing);
            od
            
            return;
        si
    si

    class Things: Iterator[int] is
        max: int;

        // FIXME: IL name override should not be needed
        @IL.name.read("get_Current")
        current: int;

        init(max: int) is
            current = -1;
            self.max = max;
        si        

        // FIXME: IL name override should not be needed
        @IL.name("MoveNext")
        move_next() -> bool is
            current = current + 1;

            return current < 50;
        si        
    si
si