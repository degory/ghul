namespace Test is
    use System.String;
    use System.Console;

    class Main is
        entry(args: String[]) static is
            @IL.entrypoint()

            Console.write_line("have args count: " + args.count);
        si
    si
si