@IL.stub()
namespace Collections is    
    @IL.name("[mscorlib]System.Collections.Generic.IEqualityComparer`1")
    trait EqualityComparer[T] is
        @IL.name("Equals")
        equals(x: T, y: T) -> bool;

        @IL.name("GetHashCode")
        hash(obj: T) -> int;
    si

si
