namespace Test is
    use Collections;

    class Main: Whatever is
        entry() static is
            @IL.entrypoint()

            let list = new LIST[int]();

            list.add(1);
            list.add(2);
            list.add(3);
            list.add(4);

            for i in list do
                System.Console.write_line("have: " + i);
            od
        si

        blah() is
            
        si        
    si
    
    trait Whatever is
        blah();
    si
si

@IL.stub()
namespace System is
    @IL.name("class [mscorlib]System.Console")
    class Console is
        @IL.name("WriteLine")
        write_line(text: String) static;
    si
si