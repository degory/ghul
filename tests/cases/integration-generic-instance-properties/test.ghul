namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let test = new TEST[int, System.String](12345, "hello");
            
            System.Console.write_line("have t: " + test.t);
            System.Console.write_line("have u: " + test.u);

            return;
        si
    si

    class TEST[T,U] is
        t: T;
        u: U;

        init(t: T, u: U) is
            self.t = t;
            self.u = u;
        si
    si
si