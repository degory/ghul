namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            System.Console.write_line("hello, world");
        si
    si    
si


@IL.stub()
namespace System is
    @IL.name("[mscorlib]System.Console")
    class Console is
        @IL.name("WriteLine")
        write_line(text: String) static;
    si
si