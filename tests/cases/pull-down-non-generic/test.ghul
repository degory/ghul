namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let c: CCCC;
            let d: DDDD;

            c.test(1234);
            d.test(1234);

            return;
        si
    si

    trait I is
        test(i: int);        
    si

    trait J is
        test(j: int);
    si
    

    class CCCC: I is
        init() is si

        test(i: int) is
            System.Console.write_line("test int: " + i);
        si
    si

    class DDDD: CCCC is
        init() is si        
    si

    class EEEE: CCCC, J is
        
    si
    
si