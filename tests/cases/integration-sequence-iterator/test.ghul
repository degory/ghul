namespace Test is
    use System.Console;

    class Main is
        entry() static is
            @IL.entrypoint()

            for s in [1, 2, 3, 4, 5] do
                if s > 1 then
                    Console.write(",");
                fi
                
                Console.write(s);
            od
            
            Console.write_line();
        si
    si
si