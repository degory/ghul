namespace Test.ParseUnary is
    use Generic;

    class Test is
        test() is
            var x: int;

            if 1 == 1
                x = 1;
            else
                x = 2;
            fi
        si        
    si
si
