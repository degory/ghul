namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let x = [1, 2, 3, 4, 5];

            System.Console.write_line("have sequence: " + x.count);

            return;
        si
    si
si