namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let c1 = new C();
            let c2 = new C(123);
            let c3 = new C("hello");

            let d1 = new D(123); // OK - D directly defines init(i: int)
            let d2 = new D("hello");


            let e1 = new E(123); // should error - no init(i: int) constructor defined directly in E
            let e2 = new E(123,456,789);
            let e3 = new E("hello");

            return;
        si
    si

    class C is
        
    si

    class D is
        init(i: int) is
        si
    si

    class E: D is
        
    si    
si