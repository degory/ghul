namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let list = new System.LIST[System.Object]();

            list.add(1);
            list.add(2);
            list.add(3);

            System.Console.write_line("got to end");
        si
    si
si

@IL.stub()
namespace System is
    @IL.name("[mscorlib]System.Console")
    class Console is
        @IL.name("WriteLine")
        write_line(text: String) static;
    si

    @IL.name("[mscorlib]System.Collections.Generic.List`1")
    class LIST[T] is
        @IL.name("Add")
        add(item: T);

        init();
    si
si