namespace Test is
    use System.Console;

    class Main is
        entry() static is
            @IL.entrypoint()

            let values = [
                new BASE(),
                new DERIVED(),
                new GENERIC[int]()
            ];

            for i in values do
                Console.write_line("call overridden() on " + i);

                i.overridden(12345);
            od            
        si
    si

    class BASE is
        init() is si

        overridden(value: int) is
            Console.write_line("BASE.overridden(" + value + ")");
        si
    si  

    class DERIVED: BASE is
        init() is si

        overridden(value: int) is
            Console.write_line("DERIVED.overridden(" + value + ")");

            super.overridden(value);
        si
    si

    class GENERIC[T]: DERIVED is
        init() is si

        overridden(value: int) is
            Console.write_line("GENERIC.overridden(" + value + ")");            

            super.overridden(value);
        si        
    si    
si