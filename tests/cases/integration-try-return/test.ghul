namespace Test is
    use System.Console;

    class Main is
        entry() static is
            @IL.entrypoint()

            let value = test(123);

            System.Console.write_line("result is:" + value);
        si

        test(value: int) -> int static is
            try
                System.Console.write_line("will return: " + value * 2);
                return value * 2;
            finally
                // FIXME: should be executed see #441
                System.Console.write_line("finally: " + value);
            yrt
        si        
    si
si