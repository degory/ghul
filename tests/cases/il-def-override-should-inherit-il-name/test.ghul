namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            return;
        si
    si

    class BASE is
        @IL.output("il.out")
        @IL.name("ActuallyTest")
        test() is
            
        si
    si

    class DERIVED: BASE is
        @IL.output("il.out")
        test() is
            
        si
        
        @IL.output("il.out")
        test(i: int) is            
        si
    si
si