namespace Test is
    use System.String;

    class Main is
        entry() static is
            @IL.entrypoint()

            return;
        si
    si

    class BASE is
        @IL.output("il.out")
        @IL.name("ActuallyTest")
        test() is
            
        si

        @IL.name("ActuallyOther")
        other(s: String) is            
        si
    si

    class DERIVED: BASE is
        @IL.output("il.out")
        test() is
            
        si

        other(s: String) is            
        si
        
        @IL.output("il.out")
        test(i: int) is            
        si
    si
si