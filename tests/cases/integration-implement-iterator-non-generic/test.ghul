namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            for t in new LIST() do
                System.Console.write_line("have thing: " + t);
            od
        si
    si

    class THING is
        init() is si
    si

    class LIST: System.Object, Collections.Iterable[THING] is
        iterator: Collections.Iterator[THING] => [new THING(), new THING(), new THING()].iterator;

        init() is si
    si
si