namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let c = new C();

            let a: System.Object = c.f(123);
            let b = c.f(456);

            System.Console.write_line("expecting 123456: " + a + b);

            let d = c.g(111);
            let e: System.Object = c.g(222); 

            System.Console.write_line("expecting 111222: " + d + e);
        si
    si

    class C is
        init() is
        si

        g(i: int) -> System.Object is return i; si
        f(i: int) -> System.Object => i;
    si
    
si