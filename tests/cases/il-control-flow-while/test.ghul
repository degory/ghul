namespace Test is
    use System;

    class Main is
        entry() static is
            @IL.entrypoint()

            let index = 0;

            @IL.output("il.out")
            while index < 10 do
                index = index + 1;
            od
            
            return;
        si
    si
si
