namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            @IL.output("il.out")
            throw 12345;

            @IL.output("il.out")
            throw "hello world";

            @IL.output("il.out")
            throw new System.Exception();
        si
    si
si