namespace Test is
    use System.String;
    use System.Console;

    class Main is
        entry(args: String[]) static is
            @IL.entrypoint()

            new Main().test(args);
        si

        init() is
        si

        test(args: String[]) is
            Console.write_line("have args");
        si
    si
si