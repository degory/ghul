namespace Test is
    class Main is
        init() static is
            @IL.entrypoint()

            let list = new Collections.LIST[int]();

            list.add(1);
            list.add(2);
        si
    si
si

@IL.stub()
namespace System is
    @IL.name("[mscorlib]System.Console")
    class Console is
        @IL.name("WriteLine")
        write_line(text: String) static;
    si
si

@IL.stub()
namespace Collections is
    @IL.name("[mscorlib]System.Collections.Generic.IList`1")
    class LIST[T]: Generic.Iterator[T] is
        @IL.name(".ctor")
        init();

        @IL.name("Add")
        add(item: T);
    si
si
