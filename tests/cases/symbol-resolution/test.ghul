namespace Test is
    use WillImport;

    class Main is
        entry() static is
            @IL.entrypoint()

            return;
        si
    si

    class DUPLICATE is
        other_func() -> DUPLICATE;


    si
    
si

namespace WillImport is
    class DUPLICATE is
        import_func() is si
    si
si
