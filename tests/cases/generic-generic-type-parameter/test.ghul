namespace Test is
    use Generic;

    class Main is
        entry() static is
            @IL.entrypoint()

            let o = new Other[Main];

            o.add()

            return;
        si
    si


    class Thing[T]: List[Iterator[T]] is
        add(stuff: List[Iterator[T]]) is
            
        si
        
    si

    class Other[T]: Thing[T] is
        init() is
            
        si

        add(stuff: List[Iterator[T]]) is
            self.add(
        si
        
        
    si    
si