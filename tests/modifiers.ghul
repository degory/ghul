namespace Tests.Modifiers is
    class SOME_CLASS is
        blah: int;
        x: int static;

        [index: int]: int static => index, = value is blah = value; si

        z: Collection.List[System.STRING] private;

        f(a: int, b: int) -> Collection.List[List[System.STRING]] protected => none;

        g(x: word, y: word) protected => x + y;
    si
si