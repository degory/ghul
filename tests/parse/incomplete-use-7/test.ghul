// missing terminating semi colon should not prevent use of namespace 
namespace Test is
    use System
    use Ghul

    class Test is
        test() is
            let s: String = "";
            let i: int = 1234;
        si
    si
si
