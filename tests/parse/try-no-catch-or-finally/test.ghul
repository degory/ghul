namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            @IL.entrypoint()

            @IL.output("il.out")
            try
                Std.error.write_line("do stuff");
            yrt
        si
    si
si