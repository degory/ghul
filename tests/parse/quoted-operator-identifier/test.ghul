namespace Test is
    use Std = IO.Std;

    use `=~ = object.equals;

    class Main is
        entry() static is
            let x = `+(123, 456);

            let e = x =~ x;

            return;
        si        
    si
si