namespace Tests.Properties is
    class SOME_CLASS is
        _prop: int;
        _count: int;

        plain;

        typed: int;

        inferred => 10;

        width: int;
        height: int;

        area: int => width * height;

        function(c: int, v: int) => c * v;

        prop: int
            => _prop,
            = value is
                _prop = value;
            si

        complex_prop: int
            is
                _count = _count + 1;
                return function(_count);
            si,

            = value is
                _count = _count - 1;
                function(_count, value);
            si
    si
si