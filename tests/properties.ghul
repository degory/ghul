namespace Tests.Properties is
    class SOME_CLASS is
        plain;

        typed: int;

        inferred => 10;

        area: int => width * height;

        prop: int
            => _prop,
            = value is
                _prop = value;
            si

        complex_prop: int
            is
                _count = _count + 1;
                return function(count);
            si,

            = value is
                _count = _count - 1;
                function(count, value);
            si
    si
si