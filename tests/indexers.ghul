namespace Tests.Indexers is
    class SOME_CLASS is
        [index: int]
            => index;

        [index: int]
            => (_element_pointer + index).peek(),
            = value is
                (_element_pointer + index).poke(value);
            si

        [index: int]: Some[int]
            is
                return thing[index].some;
            si,
            = value is
                thing[index].some = value;
            si

        names[index: int] => self.names[index];

        ages[index: int]: AGE
            => get_age(index),
            = value is
                set_age(index, value);
            si
    si
si