namespace Test is
    use Std = IO.Std;

    @IL.output("il.out")
    enum Blah is
        Bloop,
        Blip,
        Blergh,
        Bloggle = 40,
        Blanger = 50
    si
    
    class Main is
        entry() static is
            @IL.entrypoint()

            return;
        si
    si
si