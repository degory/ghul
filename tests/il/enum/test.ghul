namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    @IL.output("il.out")
    enum Blah is
        Bloop,
        Blip,
        Blergh,
        Bloggle = 40,
        Blanger = 50
    si
    
    class Main is
        entry() static is
            @IL.entrypoint()

            return;
        si
    si
si