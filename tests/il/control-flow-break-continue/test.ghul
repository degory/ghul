namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    
    class Main is
        entry() static is
            @IL.entrypoint()

            let index = 0;
            let skip = 0;

            @IL.output("il.out")
            while index < 10 do
                index = index + 1;
                break;
                skip = 1;
            od
            
            @IL.output("il.out")
            while index < 20 do
                index = index + 1;
                continue;
                skip = 2;
            od
        si
    si
si