namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class Main is
        entry() static is
            @IL.entrypoint()

            return;
        si
    si

    class BASE is
        _test_0: int;
        _test_1: string;
    si

    class DERIVED: BASE is
        init() is si

        test() is
            @IL.output("il.out")
            _test_0 = 12345;

            @IL.output("il.out")
            _test_1 = "hello, world";

            @IL.output("il.out")
            let t0: int = _test_0;

            @IL.output("il.out")
            let t1: string = _test_1;
        si
    si    
si