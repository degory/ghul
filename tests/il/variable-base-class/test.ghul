namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            return;
        si
    si

    class BASE is
        _test_0: int;
        _test_1: System.String;
    si

    class DERIVED: BASE is
        init() is si

        test() is
            @IL.output("il.out")
            _test_0 = 12345;

            @IL.output("il.out")
            _test_1 = "hello, world";

            @IL.output("il.out")
            let t0: int = _test_0;

            @IL.output("il.out")
            let t1: System.String = _test_1;
        si
    si    
si