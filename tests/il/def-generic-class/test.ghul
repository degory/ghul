namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    @IL.output("il.out")
    class Generic[T,U] is
        test(t: T) -> U is si
        
        test(u: U) -> T is si
    si
si