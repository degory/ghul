namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()
            let ex = new System.Exception("should exit process");

            @IL.output("il.out")
            throw ex;
        si
    si
si