namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    @IL.output("il.out")
    class Main is
        _test_0: int static;
        _test_1: string static;

        entry() static is
            @IL.entrypoint()

            _test_0 = 123;
            _test_1 = "hello, world";

            return;
        si
    si
si