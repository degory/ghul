namespace Test is
    use System.String;
    use System.Type2;
    use System.Console;

    class Main is
        entry() static is
            @IL.entrypoint()

            let ti: Type2;

            @IL.output("il.out")
            ti = typeof int;

            let ts: Type2;

            @IL.output("il.out")
            ts = typeof String;

            let tt: Type2;

            @IL.output("il.out")
            tt = typeof (int,int,int);
        si
    si
si