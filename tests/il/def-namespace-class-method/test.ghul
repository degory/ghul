@IL.output("il.out")
namespace Namespace is
    use System.String;

    class Class is
        init() is
        si        

        @IL.name("Main")
        main() static is
            @IL.entrypoint()
            let instance: Class;
            instance = new Class();

            instance.test("Hello World!");
        si

        test(text: String) is
            System.Console.write_line(text);
        si
        
    si
si
