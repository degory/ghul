namespace Test.Expressions is
    class A is
        stuff => 123;
    si

    class SOME_CLASS is
        some_function() is
            let a = 10 + 20;

            let b = a * 10 + a * 20;

            let c = a > b || b < a;

            let d = a >= b && b <= a;

            let e = a ^ b & b ^ a;

            let f = a | b & a;

            let h = a << b | b >> a;

            let n = new Collection.VECTOR_LIST[int](20);

            let m = cast Collection.List[int](n);

            let s = new A();

            if s? then
                s.stuff();
            fi

            if s??? then
                s.stuff();
            fi
        si
    si
si