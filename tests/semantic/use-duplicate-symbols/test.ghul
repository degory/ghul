namespace Test is
    use Other.One.property;
    use Other.Two.property;

    class Main is
        entry() static is
            return;
        si
    si
si

namespace Other is
    class One is
        property: int static;
    si

    class Two is
        property: string static;
    si
si
