namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class Main is
        start() static is
            @IL.entrypoint()

            return;
        si
    si

    class BASE is
        test() is
        si
    si

    class INTERMEDIATE: BASE is
        test() is
            
        si        
    si

    class DERIVED: INTERMEDIATE is
        test() is            
        si
    si

    trait Interface[T] is
        blah();
    si

    class BASE_2 is
        init() is            
        si
        
        blah();
    si

    class INTERMEDIATE_2: BASE_2 is
    si
    
    class DERIVED_2[T]: INTERMEDIATE_2, Interface[T] is
        init() is
            
        si        
    si    
si