namespace Test is
    use IO = System.IO2;
    use Std = System.Console;

    class Main is
        entry() static is
            @IL.entrypoint()

            let f = IO.File.create_text("test.txt");

            Std.error.write_line("hello world");

            System.IO2.File.create_text("test.txt");

            System.Console.error.write_line("hello world");

            Console.write_line();

            return;
        si
    si
si