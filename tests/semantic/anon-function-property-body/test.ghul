namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            return;
        si

        property: int -> int is
            let f = (i: int) -> int => i * 2;

            return f;
        si, = f is
            let f = (i: int) -> int => i * 2;
        si

        other: int -> int => (i: int) -> int => i * 2;
    si
si