namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class Main is
        entry() static is
            @IL.entrypoint()

            return;
        si

        property: int -> int is
            let f = (i: int) -> int => i * 2;

            return f;
        si, = f is
            let f = (i: int) -> int => i * 2;
        si

        other: int -> int => (i: int) -> int => i * 2;
    si
si