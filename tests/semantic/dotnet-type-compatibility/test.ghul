namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    use Collections;

    class Main is
        entry() static is
            @IL.entrypoint()

            // let iterable_int: Iterable[int];
            // let list_int: List[int];
            // let mutable_list_int: MutableList[int];

            // iterable_int = new LIST[int]();
            // iterable_int = list_int;
            // // list_int = mutable_list_int;

            // let x: string;

            let iterable_char: Iterable[char];

            iterable_char = "hello";

            // let o: object = "hello";

            return;
        si
    si
si