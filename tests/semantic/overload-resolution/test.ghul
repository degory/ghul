namespace Test is








    use Std = IO.Std;

        
    class Main is
        entry() static is
            @IL.entrypoint()

            test(1.0D);

            test(1.0D, 123);

            test(123, 456);

            test("hello", 123, 456.0);

            test(123, 456, 789.0);

            test("hello", "world");

            test("hello", 1, 2);

            test(new object(), 1, 2);

            test(123.0, 456, 789);

            test(123, 456, 789);

            return;
        si

        test(s: string) static is         
        si

        test(i: int) static is
        si
        
        test(o: object, p: string) static is
        si

        test(s: string, t: object) static is          
        si

        test(i: string, j: int, k: int) static is            
        si    

        test(i: object, j: int, k: int) static is
        si        
    si
si