namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let f: (int, int) -> int;

            f = test();

            return;
        si

        test() -> (int, int) -> int static is
            let capture = "hello";

            let f = (i: int, j: int) -> int is
                let x: int;
                x = i;
                let y: char;
                y = j;

                let z: int;
                z = i + j;

                let c: System.String;
                c = capture;
                let d: long;
                d = capture;

                return z;
            si;

            return f;
        si

        test2(i: int, j: int) -> int => i + j;        
    si
si