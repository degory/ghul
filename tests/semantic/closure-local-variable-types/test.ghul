namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class Main is
        entry() static is
            

            let f: (int, int) -> int;

            f = test();

            return;
        si

        test() -> (int, int) -> int static is
            let capture = "hello";

            let f = (i: int, j: int) -> int is
                let x: int;
                x = i;
                let y: char;
                y = j;

                let z: int;
                z = i + j;

                let c: string;
                c = capture;
                let d: long;
                d = capture;

                return z;
            si;

            return f;
        si

        test2(i: int, j: int) -> int => i + j;        
    si
si