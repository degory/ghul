namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class Main is
        entry() static is
            @IL.entrypoint()

            let l = new Collections.LIST[int]();

            l.non_existant_property = 1;

            l.non_existant_function(123,456);

            l.add("hello");

            l = 1234;

            let i: int;
            
            i = l;

            return;
        si
    si
si