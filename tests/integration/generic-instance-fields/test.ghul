namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let test = new TEST[int, System.String](12345, "hello");
            
            System.Console.write_line("have t: " + test.get_t());
            System.Console.write_line("have u: " + test.get_u());

            return;
        si
    si

    class TEST[T,U] is
        _t: T;
        _u: U;

        init(t: T, u: U) is
            _t = t;
            _u = u;
        si

        get_t() -> T => _t;

        get_u() -> U => _u;        
    si
si