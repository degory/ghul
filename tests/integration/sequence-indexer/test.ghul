namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let x = ["hello", "there", "world"];

            let h = x[0];
            let t = x[1];
            let w = x[2];

            System.Console.write_line("have: " + h + " " + t + " " + w);

            return;
        si
    si
si