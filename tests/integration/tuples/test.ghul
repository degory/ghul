namespace Test is
    use System.String;
    use System.Console;

    class Main is
        entry() static is
            @IL.entrypoint()

            let x = (1, 2, 3);

            let y = ("hello", "world");

            Console.write_line("int tuple is: " + x);
            Console.write_line("string tuple is: " + y);

            // Console.write_line("test in to string: " + test_int_to_string((123, 456)));
        si

        // test_int_to_string(int_tuple: (int, int)) -> (String, String) static =>
        //     (int_tuple._0.to_string(), int_tuple._1.to_string());
    si
si