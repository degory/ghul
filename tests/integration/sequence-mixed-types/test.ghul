namespace Test is
    use Std = IO.Std;
    
    class Main is
        entry() static is
            @IL.entrypoint()

            for o in [1, 2, "blah", new Main(), 5, 6, [7, 8, 9]] do
                Std.out.write_line(o);
            od
        si

        init() is
        si
    si
si