namespace Test is
    use System;

    class Main is
        entry() static is
            @IL.entrypoint()

            Console.write_line("hello" + " world");
            Console.write_line("hello " + 12345 + 67890);

            Console.write_line("goodbye " + "cruel " + "world");

            let s = "some";
            let t = "text";

            let u = s + " " + t;

            Console.write_line(u);
        si
    si
si

