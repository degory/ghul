namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    use Collections;

    class Main: Whatever is
        entry() static is
            @IL.entrypoint()

            let list = new LIST[int]();

            list.add(1);
            list.add(2);
            list.add(3);
            list.add(4);

            for i in list do
                Std.out.write_line("have: " + i);
            od
        si

        blah() is
            
        si        
    si
    
    trait Whatever is
        blah();
    si
si
