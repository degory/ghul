class Main is    
    entry() static is
        @IL.entrypoint()

        IO.Std.write_line("global function: {0}", global_function(123));

        return;
    si

    global_function(i: Ghul.int) -> int => i * 2;
si