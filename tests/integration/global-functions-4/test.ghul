use IO.Std.write_line;

_x: int;

z: int;

entry() is
    @IL.entrypoint()

    let y = 2;

    // capturing anon function:
    let f = (i: int) => i + _x + y;

    // non-capturing anon function:
    let g = (i: int) => i * 2;

    write_line("hello world: f = " + f(123) + " g = " + g(123));
si
