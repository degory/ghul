namespace Test is
    use System.Console;

    class Main is
        entry() static is
            @IL.entrypoint()
            let total = 0.0D;

            for i in 0..20 do
                total = total + cast double(i);

                Console.write_line("total is now: " + total + " average is now: " + total / cast double(i));
            od
        si
    si
si