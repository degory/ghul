namespace Test is
    use System.String;
    use System.Console;

    class Main is
        entry(args: String[]) static is
            @IL.entrypoint()

            Console.write_line("have args count: " + args.count);

            let b: Collections.Bag[String] = args;
            Console.write_line("have Bag[String] count: " + b.count);

            let l: Collections.List[String] = args;
            Console.write_line("have List[String] count: " + l.count);
        si
    si
si