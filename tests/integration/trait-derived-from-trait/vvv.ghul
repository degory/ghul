namespace Test is
    class Main is
        entry() static is
            @IL.entrypoint()

            let f = new FFFF();
            let g = new GGGG();

            System.Console.write_line("FFFF isa Base: " + isa Base(f));
            System.Console.write_line("FFFF isa SettableBase: " + isa SettableBase(f));

            System.Console.write_line("GGGG isa Base: " + isa Base(g));
        si            
    si    
si
