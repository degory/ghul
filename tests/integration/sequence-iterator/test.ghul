namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    
    class Main is
        entry() static is
            @IL.entrypoint()

            for s in [1, 2, 3, 4, 5] do
                if s > 1 then
                    Std.out.write(",");
                fi
                
                Std.out.write(s);
            od
            
            Std.out.write_line();
        si
    si
si