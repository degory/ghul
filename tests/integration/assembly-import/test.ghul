namespace Test is
    use System.Console;
    use System.Object;
    use System.String;
    
    // use Collections;

    class Main is
        entry() static is
            @IL.entrypoint()

            let x = [1, 2, 3]: Object;

            let y: Collections.List[String] = ["A", "B", "C"];

            Console.write_line("{0} {1} {2}", x);

            Console.write_line("max value is: " + int.max_value);

            let i = x.iterator;

            let c = i.current;

            let d = new String(['a']);

            i.move_next();

            i.current.get_hash_code();

            let f = System.IO.File.create("hello");

            return;
        si
    si
si
