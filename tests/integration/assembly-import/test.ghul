namespace Test is
    // @IF.not.v3() use object = System.Object;
    // @IF.not.v3() use string = System.String;
    // @IF.not.v3() use STD = System.Console;
    // @IF.not.v3() use TYPE = System.Type2;

    class Main is
        entry() static is
            @IL.entrypoint()

            let x = [1, 2, 3]: object;

            let y: Collections.List[string] = ["A", "B", "C"];

            IO.STD.error.write_line("{0} {1} {2}", x);

            IO.STD.error.write_line("max value is: " + int.max_value);

            let i = x.iterator;

            let c = i.current;
            
            i.move_next();

            i.current.get_hash_code();

            let f = IO.File.create("hello");

            IO.File.append_all_text("hello", "hello");

            return;
        si
    si
si
