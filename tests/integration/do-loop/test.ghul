namespace Test is
    use System.Console;

    class Main is
        entry() static is
            @IL.entrypoint()

            Console.write_line("start");

            let i = 0;
            do
                Console.write_line("top, i is " + i);
                
                i = i + 1;

                Console.write_line("bottom, i is " + i);

                if i > 2 then
                    Console.write_line("break");
                    break;
                fi
            od
            
            Console.write_line("end");
        si
    si
si