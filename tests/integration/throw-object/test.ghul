namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class Main is
        entry() static is
            @IL.entrypoint()

            // FIXME: cannot test this as runtime output is not consistent from one run to the next
            throw "hello world";

            Std.out.write_line("should not reach here");
        si
    si
si