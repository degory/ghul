namespace Test is
    use STD = System.Console;
    
    class Main is
        entry() static is
            @IL.entrypoint()

            STD.output.write_line("hello, world");
        si
    si    
si
