namespace Test is
    use System.Object;
    use System.Console;

    use Collections.Iterable;
    use Collections.Iterator;

    class Main is
        entry() static is
            @IL.entrypoint()

            let iterable: Iterable[int];

            iterable = 1..10;

            let iterator: Iterator[int];

            iterator = iterable.iterator;

            while iterator.move_next() do
                Console.write_line("have element: " + iterator.current);
            od
            
            iterator = iterable.iterator;

            while iterator.move_next() do
                Console.write_line("have element: " + iterator.current);
            od

            let range: Range[int];

            range = 20..30;
            Console.write_line("range from " + range.from + " to " + range.to);

            range = 5::25;
            Console.write_line("range from " + range.from + " to " + range.to);
        si
    si
si