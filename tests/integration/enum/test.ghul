namespace Test is
    enum Blah is
        Bloop,
        Blip,
        Blergh
    si
    
    class Main is
        entry() static is
            @IL.entrypoint()

            let x = Blah.Bloop;
            let y = Blah.Blergh;

            if x == y then
                System.Console.write_line("not expected: " + x + " == " + y);
            else
                System.Console.write_line("expected: " + x + " != " + y);
            fi

            x = Blah.Blip;
            y = Blah.Bloop;

            if x != y then
                System.Console.write_line("expected: " + x + " != " + y);
            else
                System.Console.write_line("not expected: " + x + " == " + y);
            fi

            x = y;

            if x == y then
                System.Console.write_line("expected: " + x + " == " + y);
            else
                System.Console.write_line("not expected: " + x + " != " + y);
            fi
        si
    si
si