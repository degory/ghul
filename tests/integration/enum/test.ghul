namespace Test is
    use Std = IO.Std;

    enum Blah is
        Bloop,
        Blip,
        Blergh
    si

    struct Ergh is
        
    si
    
    
    class Main is
        entry() static is
            

            let x = Blah.Bloop;
            let y = Blah.Blergh;

            let bf: System.Reflection.BindingFlags;
            let cf: System.Reflection.BindingFlags;

            bf == cf;

            if x == y then
                Std.out.write_line("not expected: " + x + " == " + y);
            else
                Std.out.write_line("expected: " + x + " != " + y);
            fi

            x = Blah.Blip;
            y = Blah.Bloop;

            if x != y then
                Std.out.write_line("expected: " + x + " != " + y);
            else
                Std.out.write_line("not expected: " + x + " == " + y);
            fi

            x = y;

            if x == y then
                Std.out.write_line("expected: " + x + " == " + y);
            else
                Std.out.write_line("not expected: " + x + " != " + y);
            fi
        si
    si
si