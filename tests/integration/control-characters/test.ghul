namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    
    class Main is
        entry() static is
            

            // let r = '\r';
            let n = '\n';
            let t = '\t';

            Std.out.write_line("newline is: " + cast int(n));

            Std.out.write_line("tab is: " + cast int(t));

            let test = "\r\n\t";

            for c in test do
                Std.out.write_line("have: " + cast int(c));
            od

            let split = "hello\nworld";

            Std.out.write_line(split);

            let concat = "hello" + n + "world";

            Std.out.write_line(concat);

            for c in concat do
                Std.out.write_line("have: " + cast int(c));
            od
        si
    si
si