namespace Test is
    use System.Console;

    class Main is
        entry() static is
            @IL.entrypoint()

            // let r = '\r';
            let n = '\n';
            let t = '\t';

            Console.write_line("newline is: " + cast int(n));

            Console.write_line("tab is: " + cast int(t));

            let test = "\r\n\t";

            for c in test do
                Console.write_line("have: " + cast int(c));
            od

            let split = "hello\nworld";

            Console.write_line(split);

            let concat = "hello" + n + "world";

            Console.write_line(concat);

            for c in concat do
                Console.write_line("have: " + cast int(c));
            od
        si
    si
si