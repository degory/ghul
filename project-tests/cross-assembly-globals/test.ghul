namespace Test is
    use IO.Std.write_line;
    use CrossAssemblyGlobals;

    class Main is
        entry() static is
            write_line("add_one(7): " + add_one(7));
            write_line("answer: " + answer);
        si
    si
si
