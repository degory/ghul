namespace Test is
    use ImportedDefaultTrait;

    class GREETING: IGreeter is
        init() is si
    si

    class Main is
        entry() static is
            let g = GREETING();
            g.greet();
        si
    si
si
