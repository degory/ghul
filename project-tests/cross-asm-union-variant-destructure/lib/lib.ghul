namespace LibUnion2 is
    // A union with a two-field variant — checks that cross-asm
    // tuple destructuring of variant payload preserves field order.
    union PAIR is
        ONE(value: int);
        TWO(left: int, right: int);
    si

    entry() is si
si
