// Stub entry point. analysis-protocol is a driver library — its public
// Analysis.Protocol.* types are consumed by analysis-tests and
// analysis-profiler — but the ghūl toolchain only emits executables, so a
// no-op entry() is required to satisfy the assembler.
entry() is
si
