namespace StaticMemberAccess is
    class CONTAINER is
        static static_field: int = 7;

        static static_method() -> string => "x";

        init() is si

        instance_method() -> int => 1;
    si

    unrelated_function() -> int => 99;

    run() is
        let local_variable = 1;
        CONTAINER.
    si
si
