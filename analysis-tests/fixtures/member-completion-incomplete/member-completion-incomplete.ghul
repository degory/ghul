namespace MemberCompletionIncomplete is
    class GREETER is
        init() is si

        greet() -> string => "hello";

        farewell() -> string => "goodbye";
    si

    run() is
        let g = GREETER();
        if g.
    si
si
