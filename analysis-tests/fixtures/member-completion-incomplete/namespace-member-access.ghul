namespace NamespaceMemberAccess is
    namespace Inner is
        class WIDGET is
            init() is si

            spin() -> int => 1;
        si

        namespace Deeper is
            class GADGET is
                init() is si
            si
        si
    si

    unrelated_function() -> int => 99;

    run() is
        let local_variable = 1;
        NamespaceMemberAccess.
    si
si
