namespace PartialMemberName is
    class GREETER is
        init() is si

        greet() -> string => "hello";

        greet_loudly() -> string => "HELLO";

        farewell() -> string => "goodbye";
    si

    unrelated_function() -> int => 99;

    run() is
        let local_variable = 1;
        let g = GREETER();
        g.gr
    si
si
