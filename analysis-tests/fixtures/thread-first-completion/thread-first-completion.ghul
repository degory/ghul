namespace ThreadFirstCompletion is
    use System.Math;

    double(x: int) -> int => x * 2;
    describe(x: int, prefix: string) -> string => "{prefix}{x}";
    shout(s: string) -> string => s;

    run() is
        let n = 3;
        let doubler = (x: int) -> int => x * 2;
        let a = n |> 
        let b = n |> dou
        let c = "s" |> 
        let d = n |> double();
        let e = n |> Math.
    si
si
