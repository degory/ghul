namespace RenameEnum is
    enum SEVERITY is
        LOW,
        MEDIUM,
        HIGH
    si

    class REPORT is
        level: SEVERITY;

        init(level: SEVERITY) is
            self.level = level;
        si

        describe() -> string => "report";
    si
si
