namespace HoverMultilineSignature is
    class HELPER is
        init() is si

        compile_expression_with_context(
            context: string,
            expected_type: string?,
            previous: string?,
            budget: int
        ) -> int is
            return budget;
        si

        driver() is
            compile_expression_with_context("a", "b", "c", 42);
        si
    si
si
