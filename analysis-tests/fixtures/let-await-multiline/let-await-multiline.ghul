namespace LetAwaitMultiline is
    use System.Threading.Tasks;

    fetch_int() -> Tasks.Task[int] => Tasks.Task.from_result(42);

    entry() -> Tasks.Task is
        let x = await fetch_int()
            .continue_with(t => t.result + 1);
        IO.Std.write_line("x={x}");
    si
si
