namespace PrimaryCtorUserInit is
    class BOX(value: int) is
        init(..) is
            _ready();
        si

        _ready() is
        si
    si

    entry() is
        let b = BOX(42);
    si
si
