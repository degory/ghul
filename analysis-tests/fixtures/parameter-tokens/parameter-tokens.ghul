namespace ParameterTokens is
    use IO.Std;

    describe(text: string, n: int mut) -> int is
        n = text.length;
        return n;
    si

    entry() is
        Std.write_line("{describe("hi", 0)}");
    si
si
