namespace SpreadCallSignature is
    apply[T.., U](f: T.. -> U, v: T..) -> U => f(v);

    join(a: string, b: string) -> string => "{a}{b}";

    entry() is
        let spread = apply(join, "x", "y");
        let written = apply(join, ("x", "y"));
    si
si
