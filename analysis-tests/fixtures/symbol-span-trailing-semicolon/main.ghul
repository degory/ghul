use IO.Std.write_line;

entry() is
    write_line(SCORE("alice", 42).who);
si

union OUTCOME is
    WIN(amount: int);
    LOSE;
si

struct POINT(x: int, y: int);

class SCORE(who: string, points: int);
