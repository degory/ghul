namespace ModifierKeywordTokens is
    class Shape open abstract is
        area() -> double;
    si

    class CIRCLE(
        radius: double,
        _label: string init
    ): Shape is
        area() -> double => 3.14 * radius * radius;
    si
si
