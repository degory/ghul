namespace LetPositions is
    class WIDGET is
        init() is si

        spin() -> int => 1;
    si

    class GADGET is
        init() is si
    si

    unrelated_function() -> int => 42;
    unrelated_constant: int = 99;

    in_let_after_colon() is
        let local_variable = 1;
        let x:
    si

    in_let_partial_name() is
        let local_variable = 1;
        let x: Wid
    si

    in_let_after_dot() is
        let local_variable = 1;
        let x: LetPositions.
    si
si
