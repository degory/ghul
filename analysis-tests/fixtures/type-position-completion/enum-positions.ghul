namespace EnumPositions is
    enum COLOUR is
        RED, GREEN, BLUE
    si

    class WIDGET is
        init() is si
    si

    use_enum() is
        let c: COLOUR.
    si

    use_enum_value() is
        let c = COLOUR.
    si
si
