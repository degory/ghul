namespace NameSlots is
    class WIDGET is
        init() is si
    si

    unrelated_function() -> int => 42;

    in_tuple_element_name() is
        let pair: (first: int, second: int);
    si

    in_function_literal_arg_name() is
        let fn: (arg: int) -> int;
    si
si
