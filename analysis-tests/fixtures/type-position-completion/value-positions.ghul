namespace ValuePositions is
    class WIDGET is
        init() is si

        spin() -> int => 1;
    si

    unrelated_function() -> int => 42;

    in_initializer() is
        let local_variable = 1;
        let y = loc
    si
si
