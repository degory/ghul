namespace RenameFixture is
    class WIDGET is
        _size: int;

        init(size: int) is
            _size = size;
        si

        size: int => _size;

        grow(by: int) is
            _size = _size + by;
        si
    si

    make_widget(size: int) -> WIDGET => WIDGET(size);
si
