namespace RenameFixture is
    use IO.Std;

    entry() is
        let w = WIDGET(10);
        w.grow(5);
        Std.write_line("{w.size}");
        Std.write_line("{make_widget(3).size}");
    si
si
