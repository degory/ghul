namespace Test is
    use IO.Std.write_line

    squares(xs: int[]) -> int[] =>
        [value * value for value in xs if value > 0]

    names(items: string?[]) -> int[] =>
        [item.length for item in items if item?]

    entry() is
        write_line("{squares([1, 2])}")
    si
si
