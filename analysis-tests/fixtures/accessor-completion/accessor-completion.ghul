namespace AccessorMemberCompletion is
    class WIDGET is
        label: string;

        init() is
            label = "hi";
        si

        go() -> string is
            return label.
    si
si

namespace AccessorScopeCompletion is
    class THING is
        count: int;

        init() is
            count = 1;
        si
    si

    run() is
        let t = THING();
        let n = t.count + 1;
    si
si
