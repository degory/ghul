namespace SynthesizedMemberHover is
    union UN is
        UVA

        UVB
    si

    class BASE(tag: int);

    class DERIVED(tag: int, extra: int): BASE is
        super(tag)

        describe() -> int => extra;
    si
si
