namespace Lexical.UnitTests is
    use Microsoft.VisualStudio.TestTools.UnitTesting.Assert;
    use Mock = Moq.Mock;

    use Logging;

    class DUMMY_Should is
        @test()

        init() is si
    si
si