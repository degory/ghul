namespace Test.ParseUnary is
    use Generic;

    class Main is
        expect_byte_ptr(value: byte ptr);

        init() is
            expect_word(native.puts("Hello World".toCString()));
        si        
    si
si
