
namespace Test is
    use Ghul;
    
    namespace A is
        class X is
            x: int;
        si
    si

    namespace B is
        class Y is
            y: int;
        si
    si


    namespace B.C is
        class Z is
            z: int;
        si
    si

    namespace D.E is // Test.D.E
        class W is
            w: int;
        si
    si

    namespace F.G is // Test.F.G
        class R is
            r: int;
        si
    si

    namespace F.G.H.I is
        class V is
            v: int;
        si
    si    

    class Q is
        a: A.X;

        b: B.Y;

        c: B.C.Z;

        d: D.E.W;

        v: F.G.H.I.V;
    si

    class Main is
        init() -> void is
            System.Console.error.write_line("namespaces");
        si
    si
si