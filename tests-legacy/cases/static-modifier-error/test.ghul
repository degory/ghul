namespace Test.StaticModifier is
    class Instance is
        init() is
        si

        instance_function() is
            IO.Std.err.println("instance function");
        si

        static_function() static is
            IO.Std.err.println("static function");
        si
    si

    class Main is
        init() is
            var instance = new Instance();

            Instance.instance_function();
        si
    si
si