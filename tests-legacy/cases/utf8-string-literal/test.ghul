namespace SimpleUTF8StringLiteral is
    use Ghul;
    use System;
    
    class Main is
        init() -> void is
            System.Console.error.write_line("Hello world from ghūl!");
        si
    si
si
