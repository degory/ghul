namespace Test.SimpleOperators is
    class Main is
        init() is
            var s = "\\\'\"\r\n";

            for c in s do
                IO.Std.err.println("c is " + cast int(c));
            od            
        si 
    si
si