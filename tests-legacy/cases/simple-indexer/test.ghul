namespace Test.SimpleIndexer is
    use Ghul;

    class I is
        b: int;

        init(b: int) is
            self.b = b;
        si

        [i: int]: int public
            => b + i,
            = value is
                b = i - value;
            si
    si

    class Main is
        init() is
            var t = 0;
            let ii = new I(1000);

            t = t + ii[0];
            t = t + ii[1];
            t = t + ii[2];
            t = t + ii[3];
            t = t + ii[4];

            ii[0] = 50;
            System.Console.error.write_line("ii[0]: " + ii[0]);
            ii[1] = 40;
            System.Console.error.write_line("ii[1]: " + ii[1]);
            ii[2] = 30;
            System.Console.error.write_line("ii[2]: " + ii[2]);
            ii[3] = 20;
            System.Console.error.write_line("ii[3]: " + ii[3]);
            ii[4] = 10;
            System.Console.error.write_line("ii[4]: " + ii[4]);

            System.Console.error.write_line("ii[0]: " + ii[0]);
            System.Console.error.write_line("ii[1]: " + ii[1]);
            System.Console.error.write_line("ii[2]: " + ii[2]);
            System.Console.error.write_line("ii[3]: " + ii[3]);
            System.Console.error.write_line("ii[4]: " + ii[4]);
        si
    si
si