namespace Test.SimpleIf is
    use Ghul;

    class Main is
        init() is
            let t = cast bool(1);
            let f = cast bool(0);

            var x: int;

            if t then
                x = 111;
                IO.Std.err.println("expected: " + x);
            else                
                x = 222;
                IO.Std.err.println("not expected: " + x);
            fi
        si
    si
si