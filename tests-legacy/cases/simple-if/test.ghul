namespace Test.SimpleIf is
    use Ghul;

    class Main is
        init() is
            let t = cast bool(1);
            let f = cast bool(0);

            var x: int;

            if t then
                x = 111;
                System.Console.error.write_line("expected: " + x);
            else                
                x = 222;
                System.Console.error.write_line("not expected: " + x);
            fi
        si
    si
si