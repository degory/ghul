namespace Test.TraitPropertyAssign is
    use System; 
    use Generic;

    class Main is
        init() is
            for i in 1::10 do
                var t: Trait = new Implement(i);

                IO.Std.err.println("test % is %" % [i, t.test]: Object);
            od                    
        si        
    si
    
    trait Trait is
        test: int,
            = value;
    si

    class Implement: Object, Trait is
        _test: int;

        test: int => _test, = value is _test = value; si

        init(v: int) is
            test = v;
        si
    si    
si