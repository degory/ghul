namespace Whatever is
    use System;
    use Generic;
    
    class Test[Z: List[U], U]is
        test() is
            let u: U;
            let z: Z;
    
            z.toString();
    
            let sb = new StringBuffer();

            sb.append(z);
            z.add(u);

            u = z[0];
        si
    si
si 
