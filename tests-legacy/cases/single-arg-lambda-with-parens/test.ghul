namespace Test.Defs is
    class Main is
        init() is
            var double = (i: int) -> int => i * 2;

            System.Console.error.write_line("" + double(1));
            System.Console.error.write_line("" + double(2));
            System.Console.error.write_line("" + double(3));
            System.Console.error.write_line("" + double(4));
            System.Console.error.write_line("" + double(5));
            System.Console.error.write_line("" + double(6));
        si
    si
si