namespace Test.SimpleOperators is

    class A is
        value: int;

        init(v: int) is
            self.value = v;
        si
    si

    class Main is
        init() is
            for i in 0..10 do
                System.Console.error.write_line("1: " + i);
                ;
            od

            ;

            for i in 0..10 do
                ;
                System.Console.error.write_line("2: " + i);
            od

            for i in 0..10 do
                System.Console.error.write_line("3A: " + i);
                ;
                System.Console.error.write_line("3A: " + i);
            od

            var j: int;

            j = 0;

            do
                System.Console.error.write_line("4: " + j);

                j = j + 1;

                if j > 9 then break; fi
                ;
            od

            ;

            j = 0; ; ;

            do
                ;
                System.Console.error.write_line("5: " + j);

                j = j + 1;

                if j > 9 then break; fi
            od

            ;j = 0;            

            do
                ;
                System.Console.error.write_line("6A: " + j);
                ;
                ;
                System.Console.error.write_line("6B: " + j);

                j = j + 1;

                if j > 9 then break; fi
            od
        si 
    si
si