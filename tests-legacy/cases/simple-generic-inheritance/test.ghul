namespace Test.GenericInheritance is
    use Ghul;

    class Main is
        init() is
            let jc = new J[char](cast char(123));
            let ji = new J[int](123);

            ji.q(123);
            jc.q(cast char(123));
        si
    si

    class I[E] is
        q(x: E) -> E => x;
    si

    class J[F]: I[F] is
        init(c: F) is
        si
    si
si