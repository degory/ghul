namespace Test.TraitindexAssign is
    use System; 
    use Generic;

    class Main is
        init() is
            var t: Trait = new Implement(10);

            for i in 0..t.length do
                t[i] = i * i;
                System.Console.error.write_line("test % is %" % [i, t[i]]: Object);
            od                    
        si        
    si
    
    trait Trait is
        length: int;
        [index: int]: int, = value;
    si

    class Implement: Object, Trait is
        array: int[];
        length: int => array.length;

        [index: int]: int => array[index], = value is array[index] = value; si

        init(v: int) is
            array = new int[](v);
        si
    si    
si