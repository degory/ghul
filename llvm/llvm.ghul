namespace LLVM is
    class Dummy is
        init() is
            CORE.LLVMInitializeCore(CORE.LLVMGetGlobalPassRegistry());

            IO.Std.err.println("LLVM initialized");
        si
    si
si
