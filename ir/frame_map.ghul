namespace IR is
    use System;
    use Generic;

    class FRAME_MAP is
        frames: Dict[Semantic.Symbol.BASE,Frame];

        init() is
            frames = new Map[Semantic.Symbol.BASE,Frame]();
        si

        [symbol: Semantic.Symbol.BASE]: Frame
            => frames[symbol],
            = frame is
                frames[symbol] = frame;    
            si        
    si
si