namespace IR is
    class CONTEXT is
        init() is si
    si

    class CONTEXT_FACTORY is
        create_context(function: Semantic.Symbol.Function) -> CONTEXT is
            return new CONTEXT();
        si
    si    
si
