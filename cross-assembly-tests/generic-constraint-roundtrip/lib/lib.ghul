namespace ConstraintLib is
    // A ghūl-declared generic with a `class` kind constraint. The
    // constraint is emitted into the compiled assembly's metadata.
    class HOLDER[T: class] is
        _value: T;
        init(value: T) is _value = value; si
        value: T => _value;
    si

    entry() is si
si
