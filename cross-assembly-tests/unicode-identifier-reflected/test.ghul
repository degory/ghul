namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            let чашка = Библиотека.Чашка(5);

            Std.write_line(чашка.описание());
            Std.write_line("{чашка.größe}");
        si
    si
si
