namespace Lib is
    describe(x: double) -> string => "double {x}"

    // A global variable reaches its own carrier the same way a function
    // does.
    last_described: string;

    remember(x: string) is last_described = x; si
si

namespace GlobalOverloadsLib2Entry is
    entry() is si
si
