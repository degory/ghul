namespace Lib is
    describe(x: string) -> string => "string {x}"
si

namespace Ghul.Intrinsics.Staging is
    tag(x: string) -> string => "Staging string {x}"
si

namespace GlobalOverloadsLibEntry is
    entry() is si
si
