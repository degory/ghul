namespace Cell2 is
    use Cell1.Named;
    use Cell1.USES;

    class TWO: Named is
        init() is si

        name() -> string => "two";
    si

    entry() is
        IO.Std.write_line(USES().greet(TWO()));
    si
si
