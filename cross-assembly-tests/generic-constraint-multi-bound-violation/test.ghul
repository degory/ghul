namespace Test is
    use MultiBoundViolation;

    // `Partial` satisfies `Repo`'s first bound but not its second: the
    // instantiation is rejected naming the missing bound.
    class Main is
        entry() static is
            let repo = Repo[Partial](Partial());
            return;
        si
    si
si
