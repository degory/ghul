// One step of an interactive session: a file of top-level statements
// compiled with --submission. Neither type says `open`, so both are
// closed, as they would be in a file. A later step of the same session
// may extend them; anything else may not.
class Animal abstract is
    init() is si

    speak() -> string => "..."
si

trait Greeter is
    greet() -> string;
si


let ready = true
