namespace Later is
    use IO.Std.write_line

    use earlier.Animal
    use earlier.Greeter

    class CAT: Animal, Greeter is
        init() is super.init() si

        speak() -> string => "meow"
        greet() -> string => "hello from a cat"
    si

    entry() is

        let pet: Animal = CAT()
        let greeter: Greeter = CAT()

        write_line(pet.speak())
        write_line(greeter.greet())
    si
si
