// One step of an interactive session: a file of top-level statements
// compiled with --submission. Neither type says `open`, and a later step,
// which is another assembly, extends both.
class Animal abstract is
    init() is si

    speak() -> string => "..."
si

trait Greeter is
    greet() -> string;
si


let ready = true
