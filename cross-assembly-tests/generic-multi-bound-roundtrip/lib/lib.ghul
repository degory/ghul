namespace MultiBoundLib is
    trait NAMED open is
        name() -> string;
    si

    trait SIZED open is
        size() -> int;
    si

    describe[T: NAMED /\ SIZED](x: T) -> string => "{x.name()}/{x.size()}";
si
