namespace Test is
    use IO.Std.write_line;
    use CrossAssemblyOperatorGlobals;

    class Main is
        entry() static is
            write_line("declaring assembly: {display_in_declaring_assembly(42)}");

            // Named by its assembly, because `Ghul.$` is in scope too
            // and declares the same `object?` parameter, so an
            // unqualified call picks between them by declaration order.
            // The backtick separates the dot from the name; without it
            // `.$` scans as one operator.
            write_line("dollar: {CrossAssemblyOperatorGlobals.`$(42)}");
            write_line("dollar within name: {3 <$> 4}");
        si
    si
si
