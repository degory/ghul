use earlier.count
use earlier.label

// A later step redefining both names in terms of what they held: inside
// its own initializer each name means the earlier step's variable, and
// the second changes type as it goes.
let count = count + 5
let label = (label, count)
