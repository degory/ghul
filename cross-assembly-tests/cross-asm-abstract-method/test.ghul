namespace App is
    use Lib.SHAPE;

    class SQUARE: SHAPE is
        _side: int;

        init(side: int) is
            super.init();

            _side = side;
        si

        area() -> int => _side * _side;
    si

    entry() is
        IO.Std.write_line(SQUARE(4).describe());
    si
si
