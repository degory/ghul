namespace SetLib is
    use Collections

    evens(limit: int) -> Set[int] is
        let result = SET[int]()

        for i in 0..limit do
            if i % 2 == 0 then
                result.add(i)
            fi
        od

        return result
    si

    tags() -> MutableSet[string] => SET[string]()
si
