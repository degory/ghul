namespace Test is
    use IO.Std.write_line;
    use NestedInterfaceArgument.Outer_IInner;

    use NestedInterfaceArgument.IMPL;

    class Main is
        entry() static is
            let x: Outer_IInner[string?] = IMPL();

            x.value = "held";

            write_line("x.value: {x.value}");
        si
    si
si
