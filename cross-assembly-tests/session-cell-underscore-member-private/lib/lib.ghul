class POINT(x: int, y: int) is
    _secret: int

    init(..) is
        _secret = 7
    si

    secret: int => _secret
si

_helper(n: int) -> int => n + 1

class _HIDDEN is
    value: int

    init() is
        value = 42
    si
si
