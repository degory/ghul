// An underscore member stays private to its class across REPL cells, as
// within one file: an earlier cell's assembly-private definitions are
// visible to a later one, but a member's own rule still applies.
use IO.Std.write_line
use cell_two.POINT

let p = POINT(1, 2)

write_line("{p._secret}")
