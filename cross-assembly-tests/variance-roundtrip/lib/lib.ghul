namespace VarianceLib is
    // A covariant trait declared in a separate ghūl library. The `out`
    // variance is emitted into the library's assembly metadata and
    // read back when the library is imported — a variance round-trip.
    trait STREAM[T: out] is
        head() -> T;
    si

    entry() is si
si
