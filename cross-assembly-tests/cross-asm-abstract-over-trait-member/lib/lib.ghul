namespace Lib is
    trait Renderable open is
        render() -> string;

        describe() -> string => "described";
    si

    class Expr: Renderable abstract open is
        init() is si

        render() -> string;
    si
si
