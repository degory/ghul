namespace System is
    @IL.stub()

    @IL.name("class [mscorlib]System.Lazy`1")
    class LAZY[T] is
        @IL.name.read("get_Value")
        value: T;
    
        @IL.name(".ctor")
        init(create: () -> T);
    si
si
