namespace System.Text is
    @IL.stub()
    @IL.name("class [mscorlib]System.Text.UTF8Encoding")
    class UTF8Encoding: System.Text.Encoding,System.Cloneable is
        @IL.name(".ctor")
        init();
        @IL.name(".ctor")
        init(encoder_should_emit_u_t_f8_identifier: bool);
        @IL.name(".ctor")
        init(encoder_should_emit_u_t_f8_identifier: bool, throw_on_invalid_bytes: bool);
    si
si