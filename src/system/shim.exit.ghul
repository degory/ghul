namespace Shim is
    class EXIT is
        @IF.legacy()
        exit(status: int) static is
            native.exit(status);
        si

        @IF.dotnet()
        exit(status: int) static is
            System.Environment.exit(status);
        si        
    si
si

@IF.dotnet()
namespace System is
    @IL.stub()

    @IL.name("class [mscorlib]System.Environment")
    class Environment is
        @IL.name("Exit")
        exit(status: int) static;
    si
si

