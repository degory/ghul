namespace Driver is
    use Collections.Iterable;
    use Collections.LIST;

    trait PathConfig is
        install_folder: string;

        library_prefix: string public;

        ilasm_path: string;        

        get_library_locations(directories: Iterable[string]) -> Iterable[string];

        get_library_location(directory: string) -> string;
    si    
si
