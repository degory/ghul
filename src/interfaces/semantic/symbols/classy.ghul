namespace Semantic.Symbols is
    use IO.Std;

    use System.Text.StringBuilder;
    
    use Collections.SET;

    use Logging;
    use Source;

    use IR.Values.Value;

    use Types.Type;

    trait Classy: Symbol is
    si
si