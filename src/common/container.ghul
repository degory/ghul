namespace IoC is
    use Autofac;

    class CONTAINER is
        _scope: IComponentContext;

        instance: CONTAINER static;
        
        init(scope: IComponentContext) is
            _scope = scope;
        si

        get[T]() -> T is
            ResolutionExtensions.resolve`[T](_scope);
        si
    si    
si
