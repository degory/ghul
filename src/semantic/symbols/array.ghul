namespace Semantic.Symbols is
    use IO.Std;
    
    use IoC;
    use Logging;
    use Source;

    use Types.Type;

    class ARRAY: GENERIC is
        init(location: LOCATION, symbol: Symbols.Classy, element_type: Type) is
            super.init(location, symbol, new Collections.LIST[Type]([element_type]));
        si
    si
si