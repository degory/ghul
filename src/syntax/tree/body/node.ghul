namespace Syntax.Trees.Bodies is
    use System;

    use Source;

    class Body: Trees.Node  is
        is_block: bool => false;
    
        init(location: LOCATION) is
            super.init(location);
        si
    si
si
