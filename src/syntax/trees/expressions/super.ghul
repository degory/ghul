namespace Syntax.Trees.Expressions is
    use Source;

    class SUPER(location: LOCATION): Expression is
        super(location);

        accept(visitor: Visitor) is
            visitor.visit(self);
        si
    si
si
