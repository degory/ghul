namespace Syntax.Trees.Bodies is

    use Source;

    class Body: Trees.NodeBase  is
        is_block: bool => false;
    
        init(location: LOCATION) is
            super.init(location);
        si
    si
si
