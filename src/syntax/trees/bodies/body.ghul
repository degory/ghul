namespace Syntax.Trees.Bodies is

    use Source;

    class Body(location: LOCATION): Trees.Node abstract is
        is_block: bool => false;
        is_null: bool => false;

        super(location);
    si
si
