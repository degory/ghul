namespace Syntax.Trees.Bodies is

    use Source;

    class Body(location: LOCATION): Trees.Node is
        is_block: bool => false;
        is_null: bool => false;

        super(location);
    si
si
