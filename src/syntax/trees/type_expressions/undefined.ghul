namespace Syntax.Trees.TypeExpressions is
    use System.NotImplementedException;

    use IO.Std;

    use Source;

    use Semantic.Types.StandardTypes;

    class UNDEFINED: TypeExpression  is
        type: Semantic.Types.Type public => IoC.CONTAINER.instance.get`[StandardTypes]().error_type, = value is si
        is_poisoned: bool => true, = value is si

        init(location: LOCATION) is
            super.init(location);
        si

        name: string is
            return "???";
        si

        accept(visitor: Visitor) is
            visitor.visit(self);
        si

        copy() -> TypeExpression => cast UNDEFINED(clone());
    si
si
