namespace Syntax.Parsers is
    enum UNWIND_TARGET is
        // unwind exception type alone isn't enough to get us to the
        // correct parser invocation, so we use a target to indicate
        // the level of the parser stack we want to unwind to

        GLOBAL,
        NAMESPACE,
        CLASSY,
        FUNCTION,
        MEMBER,
        BLOCK
    si
si