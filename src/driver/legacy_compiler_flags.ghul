namespace Driver is
    use System;
    
    class LEGACY_COMPILER_FLAGS is
        _flags: Collections.MutableList[String];

        flags: Collections.Iterable[String] => _flags;

        init() is
            _flags = new Collections.LIST[String]();
        si
    si
si