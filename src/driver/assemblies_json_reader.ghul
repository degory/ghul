namespace Driver is
    use IO.File;

    class ASSEMBLIES_JSON_READER is
        init() is
            let assemblies_string = File.read_all_text(".assemblies.json");

            // let assemblies_json = Newtonsoft.Json.JsonConvert.deserialize_object`[ASSEMBLIES_JSON](assemblies_string);
        si
    si

    class ASSEMBLIES_JSON is
        assemblies: string[];
    si
si
