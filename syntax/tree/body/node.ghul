namespace Syntax.Tree.Body is
    use System;

    use Source;

    class NODE: Tree.NODE  is
        is_block: bool => false;
    
        init(location: LOCATION) is
            super.init(location);
        si
    si
si
