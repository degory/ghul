namespace Syntax is
    namespace Tree is
        namespace Statement is
            use Generic;
            use Source;
            class RETURN : NODE  is
                expression: Expression.NODE;
                init(location: LOCATION, expression: Expression.NODE) -> void is
                    super.init(location);
                    self.expression = expression;
                si

                accept(visitor: Visitor) -> void is
                    visitor.visit(self);
                si

                walk(visitor: Visitor) -> void is
                    if !visitor.pre(self) then
                        if expression != null then
                            expression.walk(visitor);
                        fi
                    fi
                    accept(visitor);
                si

            si

        si
    si
si
