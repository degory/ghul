namespace Syntax is
    namespace Tree is
        namespace Statement is
            use Source;
            class NODE : Tree.NODE  is
                init(location: LOCATION) -> void is
                    super.init(location);
                si

            si

        si
    si
si
