namespace Syntax is
    namespace Tree is
        namespace Expression is
            use Generic;
            use Source;
            class SUPER : NODE  is
                init(location: LOCATION) is
                    super.init(location);
                si

                accept(visitor: Visitor) is
                    visitor.visit(self);
                si

            si

        si
    si
si
