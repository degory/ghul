namespace Syntax is
    namespace Tree is
        namespace Expression is
            use System;
            use Generic;
            use Source;
            class NODE : Tree.NODE  is
                type: Type.NODE public;
                init(location: LOCATION) -> void is
                    super.init(location);
                si

                accept(visitor: Visitor) -> void is
                    visitor.visit(self);
                si

            si

        si
    si
si
