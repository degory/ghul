namespace Syntax.Tree.Expression is
    use System;
    use Generic;

    use Source;

    class NODE: Tree.NODE is
        value: Value.BASE public;

        init(location: LOCATION) is
            super.init(location);
        si

        accept(visitor: Visitor) is
            visitor.visit(self);
        si
    si
si
