namespace Syntax is
    namespace Tree is
        namespace Expression is
            use Generic;
            use Source;
            class NULL : NODE  is
                init(location: LOCATION) -> void is
                    super.init(location);
                si

                accept(visitor: Visitor) -> void is
                    visitor.visit(self);
                si

            si

        si
    si
si
