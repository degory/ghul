namespace Driver is
    use System;

    class SOURCE_FILE_CATEGORIZER is
        is_legacy(file_name: String) -> bool static =>
            file_name.endsWith(".l");

        is_ghul(file_name: String) -> bool static =>
            file_name.endsWith(".ghul");                
    si
si