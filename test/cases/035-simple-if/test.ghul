namespace Test.SimpleIf is
    use Ghul;

    class Main is
        init() is
            let t = cast bool(1);
            let f = cast bool(0);

            var x: int;

            if t then
                x = 111;
            else
                x = 222;
            fi
        si
    si
si