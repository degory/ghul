namespace Test is
    trait Iterator[T]: Iterable[T] is
        next() -> bool;
    si

    trait Iterable[T] is
        iterator: Iterator[T];

        count() -> word;
        filter(condition: T -> bool) -> Iterator[T];
        map[U](project: T -> U) -> Iterator[U];
        find(condition: T -> bool) -> T ref; 
        reduce[U](initial: U, apply: T -> U) -> U;
        partition(condition: T -> bool) -> Iterator[(T, T)];
    si

    class Main is
        init() is
            // test code here
        si
    si
si