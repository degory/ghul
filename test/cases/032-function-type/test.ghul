namespace Test.Function is
    use Ghul;

    class Main is
        init() is
            var x: (int, int, int);
            var f: FUNCTION_3[int,int,int,int];
            var t: (int, int, int) -> int;
            t = (a: int, b: int, c: int) -> int => a + b + c;
            f = t;
        si
    si
si
