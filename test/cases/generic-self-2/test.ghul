namespace Test.GenericSelf is
    use System;
    use Ghul;

    class Test[T] is
        test_generic_self() is
            let i: Other[Test[T]] = new Other[Test[T]](self);
            let j: Test[T];
            let k: Other[Test[T]] = new Other[Test[T]](j);

            k.test(self);
            k.test(j);
        si
    si

    class Other[Q] is
        init(q: Q) is
            
        si
        
        test(q: Q) static is

        si
    si    
si
