namespace Test.SimpleGenric is
    use Ghul;

    class Main is
        init() is
            let ji = new J[int](123);
        si
    si

    class I[E] is
        q(x: E) -> E => x;
    si

    class J[F]: I[F] is
        init(c: F) is
        si
    si
si