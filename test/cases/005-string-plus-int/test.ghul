namespace Test is
    use Ghul;

    class Main is
        init() -> void is
            let i: int = 10;
            let j: int = 20;

            let k = i + j;

            IO.Std.err.println("hello " + i + " + " + j + " = " + k);
        si
    si
si