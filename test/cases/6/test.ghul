namespace Root.Test is
    use Ghul;
    use Generic;

    use AAAAAA;

    class Main is
        init() -> void is
            var r: Ghul.int;
            var s: Ghul.byte;
            var u: Ghul.word;
            var aa: int[][] ref;
            var ab: AAAAAA.BBBBBB;
            var bb: BBBBBB;
            
            let first = 111 * 222 + 333 * 444 + 555 * 666;

            let a = 10;
            let b = a + 20;
            let c = a + b + 30;
            let d = a + b + c + 40;
            let e = a + b + c + d + 50;

            let x = a * b + c * d;
            let y = e - a * b - c * d;

            let v = new Vector[int](10);
            v.add(123);
            let w = v[0 * 0 + 0 * 0];
        si
    si
si

namespace Root.AAAAAA is
    class BBBBBB is
        c() -> BBBBBB is

        si
    si
si