namespace Test is
    use Ghul;

    class Main is
        void init() is
            let first = 111 * 222 + 333 * 444 + 555 * 666;

            let a = 10;
            let b = a + 20;
            let c = a + b + 30;
            let d = a + b + c + 40;
            let e = a + b + c + d + 50;

            let x = a * b + c * d;
            let y = e - a * b - c * d;
        si
    si
si