namespace Test.Defs is
    class Main is
        init() is
            var double = (i: int) -> int => i * 2;

            IO.Std.err.println("" + double(1));
            IO.Std.err.println("" + double(2));
            IO.Std.err.println("" + double(3));
            IO.Std.err.println("" + double(4));
            IO.Std.err.println("" + double(5));
            IO.Std.err.println("" + double(6));
        si
    si
si