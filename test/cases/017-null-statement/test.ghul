namespace Test.SimpleOperators is

    class A is
        value: int;

        init(v: int) is
            self.value = v;
        si
    si

    class Main is
        init() is
            for i in 0..9 do
                IO.Std.err.println("1: " + i);
                ;
            od

            ;

            for i in 0..9 do
                ;
                IO.Std.err.println("2: " + i);
            od

            for i in 0..9 do
                IO.Std.err.println("3A: " + i);
                ;
                IO.Std.err.println("3A: " + i);
            od

            var j: int;

            j = 0;

            do
                IO.Std.err.println("4: " + j);

                j = j + 1;

                if j > 9 then break; fi
                ;
            od

            ;

            j = 0; ; ;

            do
                ;
                IO.Std.err.println("5: " + j);

                j = j + 1;

                if j > 9 then break; fi
            od

            ;j = 0;            

            do
                ;
                IO.Std.err.println("6A: " + j);
                ;
                ;
                IO.Std.err.println("6B: " + j);

                j = j + 1;

                if j > 9 then break; fi
            od
        si 
    si
si