namespace Test.Range is
    class Main is
        init() is
            for n in -9..9 do
                IO.Std.err.println("n: %, range: %" % [n, 0..n]);

                for i in 0..n do
                    IO.Std.err.println("% of %" % [i, 0..n]);
                od
            od
        si 
    si
si