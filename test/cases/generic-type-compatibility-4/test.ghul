namespace Whatever is
    use System;
    
    class Test[Z] is
        test() is
            let k: Z;
    
            k.toString();
    
            let sb = new StringBuffer();

            sb.append(k);
        si
    si    
si 
