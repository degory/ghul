namespace Test.ParseUnary is
    use Generic;

    class Main is
        check_int(value: int);

        puts(s: char ptr) -> int native;

        init() is
            let result = puts("Hello World!".toCString());

            check_int(result);
        si
    si
si
