namespace Test.SimpleIndexer is
    use Ghul;

    class I[E] is
        b: E;

        init(b: E) is
            self.b = b;
        si

        f(x: E) -> E is
            return x;
        si
    si

    class Main is
        init() is
            var ii: I[int];
            ii = new I[int](456);
            ii.f(123);
        si
    si
si