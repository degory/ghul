namespace Test.SimpleProperty is
    use Ghul;

    class P is
        p: int public;

        init() is
        si
    si

    class Main is

        init() is
            var q: int;
            let p = new P();

            p.p = 123;

            q = p.p;
        si 
    si
si