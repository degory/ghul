namespace Test.TypesExperiment is
    use Ghul;

    interface I is
        function(i: byte, j: byte);
        function(i: int, j: int);
        function(i: int, j: ushort);
    si

    interface J: I is
        function(i: word, j: word);
    si

    class Main is
        init() is
            var iii: I;
            var jjj: J;

            iii.function(111, 222);
            jjj.function(111, 222);
        si 
    si
si