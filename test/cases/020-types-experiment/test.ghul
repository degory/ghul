namespace Test.TypesExperiment is
    class T is
        _a: int;
        
        a: int
            => _a,
            = value is
                _a = value;
            si

        b: int
            => a,
            = value is
                a = value;
            si

        [i: int]: int
            => i * 2,
            = value is
                a = i * value;
            si            

        init() is si
    si

    class Main is
        init() is
            var i: I;

            i = new C();

            for n in 0..9 do
                IO.Std.err.println("f(" + n + ") => " + i.f(n));
                IO.Std.err.println("g(" + n + ") => " + i.g(n));
            od
        si 
    si
si