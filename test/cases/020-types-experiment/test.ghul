namespace Test.TypesExperiment is
    use Ghul;

    interface I is
        function(i: int, j: int);
    si
    
    class T: I is
        some_value: int;
        other_value: int;

        function(i: int, j: int) is
            var x = some_value;
            var y = other_value;

            var z = x + y;
        si

        init() is si
    si

    class Main is
        init() is
            var i: I;

            i = new T();

            for m in 0..9 do
                for n in 0..9 do
                    IO.Std.err.println("f(" + m + "," + n + ") => " + i.function(m, n));
                od
            od
        si 
    si
si