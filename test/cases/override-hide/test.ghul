namespace Test.ParseUnary is
    use System; 
    use Generic;

    test(a: bool, b: bool) -> char;
    test(a: int, b: int) -> bool;

    class Test is
        test(a: bool, b: bool) -> char;

        test() is
            test(true, true);
            test(111, 222);
        si
    si
si