namespace Test.Closure is
    class Main is
        init() is
            var t = 100;

            var f = (i:int) -> int use t => t + i;

            for i in 0..9 do
                IO.Std.err.println("f(%) => %" % [i, f(i)]: System.Object);
            od
        si 
    si
si