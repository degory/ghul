namespace Test40 is
    use Ghul;
    
    class Main is
        init() -> void is
            let x = 10;
            let y = 20;

            var z: int;

            z = x + y;
            
            IO.Std.err.println("Hello world!");   
        si
    si
si
