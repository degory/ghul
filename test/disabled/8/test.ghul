namespace A.B.C is
    class X is
        x() => 0;
    si
si

namespace A.B.D is
    class Y is
        y() => 0;
    si
si

namespace A.B.E is
    class Z is
        z() => 0;
    si
si

namespace Test1 is
    use A;

    class T is
        t() is
            var c: B.C;
            var d: B.D;
            var e: B.E;
        si
    si
si