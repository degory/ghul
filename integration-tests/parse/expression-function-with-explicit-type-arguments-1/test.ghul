namespace Test is
    use Std = IO.Std;

    class Main is
        function[T](t: T) -> T static;
        function[T]() -> T static;
    si

    entry() static is
        let b: int = Main.function `[int]();
        let c: (int, int) = Main.function `[(int, int)]((1, 2));

        return;
    si
si