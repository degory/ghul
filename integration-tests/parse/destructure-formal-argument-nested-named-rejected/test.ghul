struct POINT is
    x: int; y: int;
    init(x: int, y: int) is self.x = x; self.y = y; si
    deconstruct(a: int ref, b: int ref) is
        a! = x;
        b! = y;
    si
si

f((a: int, (px = a, py = b): POINT): (int, POINT)) -> int => a + px + py;

entry() static is
    return;
si
