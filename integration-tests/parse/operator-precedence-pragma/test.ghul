@precedence("##", "addition")
@precedence("%%", "16")
@precedence("!!!", "99")
namespace Test is
    use Std = IO.Std;

    ##(a: int, b: int) -> int => a + b;

    ∑(value: Collections.Iterable[int]) -> int;

    class Main is
        entry() static is
            

            let x = 1 ## 2;

            let y = "a" ## "b";

            let z = 123 @@ 456;

            let w = "hello" %% "goodbye";

            let s = ∑[1, 2, 3, 4];

            let t = ∑["a", "b"];

            let b = true \/ false;

            let bb = false ∧ false;

            let u = 0xF0F0 ∪ 0x0F0F;

            return;
        si
    si
si