worker(n: int) -> int is
    let total mut = 0
    try
        case n
        when 1 then
            total = 1
        else
            total = 2
        esac
    catch caught: System.Exception
        else
            total = 3
        esac
    catch caught: System.Exception
        total = 4
    yrt
    total = total + undefined_tail
    return total
si
