class COUNTER is
    init() is si

    count(limit: int) -> int
        let total mut = 0

        while total < limit do
            total = total + 1
        od

        total = total + undefined_x
        return total
    si

    after() -> int => undefined_y
si
