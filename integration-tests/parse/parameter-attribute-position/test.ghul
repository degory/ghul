class SIMPLE: System.Attribute is
    init() is si
si

class BOX(@SIMPLE() x: int) is
si

use_in_let() is
    let @SIMPLE() y = 5;

    IO.Std.write_line("{y}");
si

class WITH_SECONDARY(x: int, y: int) is
    init(@SIMPLE() .., extra: int) is si
si

entry() static is
    return;
si
