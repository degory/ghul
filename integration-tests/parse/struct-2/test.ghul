namespace TEST is








    use Std = IO.Std;

    struct TEST: Collections.Iterable[char] is
        init() is
        si

        @IL.name.read("GetEnumerator")
        iterator: Collections.Iterator[char] => "Hello World".iterator;
    si
si