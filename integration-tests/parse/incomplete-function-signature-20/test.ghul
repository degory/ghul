namespace Test is
    use IO.Std.write_line;

    test_A() is
    si

    test_C(a: int b: int) is
        let x = a * 10;
        let y = b * 10;
    si

    entry() is
        test_C(111, 222);
    si
si
