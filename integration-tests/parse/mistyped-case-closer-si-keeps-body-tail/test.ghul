class COUNTER is
    init() is si

    pick(n: int) -> int is
        let total mut = 0

        case n
        when 0 then
            total = 1
        else
            total = 2
        si

        total = total + undefined_x
        return total
    si
si
