// generic union only unit variants 
union ThingA[T, U] is
    NONE;
    NOTHING;
    NIL;
si


// generic union with unit and non-unit variants
union ThingB[T, U] is
    NONE;
    NOTHING;
    NIL;
    SOME(a: T);
    SOME2(a: T, b: U);
    SOME3(a: T, b: U, c: U);
si

