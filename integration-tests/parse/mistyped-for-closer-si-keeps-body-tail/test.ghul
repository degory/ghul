class COUNTER is
    init() is si

    count(limit: int) -> int is
        let total mut = 0

        for i in 0..limit do
            total = total + i
        si

        total = total + undefined_x
        return total
    si
si
