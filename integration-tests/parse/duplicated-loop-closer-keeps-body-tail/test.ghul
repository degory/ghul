class COUNTER is
    init() is si

    count(limit: int) -> int is
        let total mut = 0

        while total < limit do
            total = total + 1
        od
        od

        total = total + undefined_x
        return total
    si
si
