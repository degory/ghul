namespace Test is
    use Std = IO.Std;

    class Main[T] is
        function() -> T;
        function(t: T) -> T;

        property: T;

        entry() static is
            let x = Main [int].property;

            return;
        si
    si
si