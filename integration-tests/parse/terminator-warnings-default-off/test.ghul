// Neither terminator-style warning fires by default: written end-of-line
// terminators and inferred boundaries are both accepted silently.
use IO.Std.write_line;

entry() is
    let a = 1;
    let b = 2
    write_line("{a + b}")
si
