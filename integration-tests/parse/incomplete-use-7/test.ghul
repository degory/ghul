// missing terminating semi colon should not prevent use of namespace 
namespace Test is








    use Std = IO.Std;

    use System.Threading
    use Collections

    class Test is
        test() is
            let s: LIST[string];
            let i: MAP[int,string];
        si
    si
si
