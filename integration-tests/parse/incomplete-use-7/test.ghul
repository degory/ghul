// missing terminating semi colon should not prevent use of namespace 
namespace TEST is








    use Std = IO.Std;

    use System.Threading
    use Collections

    class TEST is
        test() is
            let s: LIST[string] mut;
            let i: MAP[int,string] mut;
        si
    si
si
