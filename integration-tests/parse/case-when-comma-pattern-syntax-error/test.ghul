namespace T is
    union Shape is
        SQUARE;
        TRIANGLE;
        CIRCLE;
    si

    class M is
        f(s: Shape) is
            // A comma-separated list in a `when` arm holds value-equality
            // expressions; a list of binding patterns is not grammatical.
            // It must be rejected with a located syntax error, not fall
            // through to expression-list parsing and reach IL generation.
            case s
            when q: Shape.SQUARE, t: Shape.TRIANGLE then
            when c: Shape.CIRCLE then
            esac
        si
    si
si
