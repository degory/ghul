namespace TEST.RecoveryStrayTokenInLoopBody is
    collatz(n: int) -> int is
        let current mut = n

        while current > 1 do
            )

            current = if current % 2 == 0 then current / 2 else 3 * current + 1 fi
        od

        return current
    si

    next_definition() -> int => undefined_after_the_damage
si
