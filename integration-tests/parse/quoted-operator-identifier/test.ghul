namespace Test is
    use Std = IO.Std;

    use `=~ = object.equals;

    class Main is
        `++ : int static;

        entry() static is
            `++ = 10;

            let x = `+(123, 456);

            let e = x =~ x;

            return;
        si        
    si
si