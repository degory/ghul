namespace T is
    union Shape is
        SQUARE;
        TRIANGLE;
        CIRCLE;
    si

    class M is
        f(s: Shape) is
            // A comma-separated list of binding patterns in one `when`
            // arm (or-patterns) is not implemented. It must be rejected
            // with a located diagnostic, not fall through to
            // expression-list parsing and reach IL generation.
            case s
            when q: Shape.SQUARE, t: Shape.TRIANGLE then
            when c: Shape.CIRCLE then
            esac
        si
    si
si
