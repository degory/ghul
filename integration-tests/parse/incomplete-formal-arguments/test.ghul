namespace Test is
    class FOO is
        // An incomplete formal-argument list that runs off into the
        // start of the following function signature: the parser rolls
        // forward to the next signature and reports the incomplete
        // arguments instead of aborting.
        first(a: int,

        second() is
        si
    si

    class Main is
        entry() static is
        si
    si
si
