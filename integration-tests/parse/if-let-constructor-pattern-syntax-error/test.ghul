namespace T is
    union Opt is
        SOME(value: int);
        NONE;
    si

    class M is
        f(o: Opt) is
            // A constructor-argument group after the ascribed type is not
            // grammatical in an `if let` or `while let` clause either. The
            // argument group is consumed so the rest of the clause still
            // parses, keeping the statement recognised as a conditional
            // definition rather than re-read as a let-in expression.
            if let v: Opt.SOME(0) = o then
            fi

            while let w: Opt.SOME(0) = o do
            od
        si
    si
si
