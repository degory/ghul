class COUNTER is
    init() is si

    countlimit: int) -> int is
        let total mut = 0

        while total < 3 do
            total = total + 1
        od

        total = total + undefined_x
        return total
    si

    after() -> int => undefined_y
si
