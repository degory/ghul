// Each inferred statement boundary is reported under missing-semicolon;
// written terminators draw nothing.
entry() is
    let a = 1
    let b = 2;
    let c = a + b
    IO.Std.write_line("{c}")
si
