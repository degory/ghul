namespace TEST.RecoveryMissingCloserReportedOnce is
    deep(n: int) -> int is
        let total mut = n

        for step in 1..4 do
            if total > 0 then
                case total
                when 0 then
                    total = total + 1
                else
                    total = total - 1
                esac
        od

        return total
    si

    next_definition() -> int => undefined_after_the_damage
si
