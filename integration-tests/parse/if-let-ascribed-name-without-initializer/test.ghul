namespace T is
    union Opt is
        SOME(value: int);
        NONE;
    si

    class M is
        f(o: Opt) is
            // The leaf-name shorthand lifts its variable out of a member
            // path, so an ascribed bare name has no path to take a name
            // from and what is missing is the initializer. Reported as
            // such, and with the scrutinee dropped so an undefined name
            // does not go on to be reported once per resolution pass.
            if let a: Opt.SOME then
            fi

            if let b = o, c: Opt.NONE then
            fi
        si
    si
si
