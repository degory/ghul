namespace Test is
    class BROKEN is
        _id: int static;

        log(text: string);

        next_name: string static is
            let result = ""'.address."{_id}"'"";

            return result;
        si

        gen(name: string) is
            log(""stloc "{name}"");
            log(""ldloca "{name}"");
        si

        to_string() -> string => ""address:["{_id}"]("{_id}")"";
    si
si
