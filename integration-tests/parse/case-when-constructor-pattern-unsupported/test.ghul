namespace T is
    union Opt is
        SOME(value: int);
        NONE;
    si

    class M is
        f(o: Opt) is
            // A right-side constructor pattern in a `when` arm is not
            // implemented (the outstanding scope of #1458). It must be
            // rejected with a located diagnostic, not fall through to
            // expression-list parsing and reach IL generation (#1810).
            case o
            when _: Opt.SOME(0) then
            when v: Opt.SOME then
            when _: Opt.NONE then
            esac
        si
    si
si
