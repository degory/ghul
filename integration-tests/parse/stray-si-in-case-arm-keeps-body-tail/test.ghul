class COUNTER is
    init() is si

    pick(n: int) -> int is
        let total mut = 0

        let picked = case n
            when 0 then
                si
                total + 1
            else
                total + 2
            esac

        total = total + undefined_x
        return picked
    si
si
