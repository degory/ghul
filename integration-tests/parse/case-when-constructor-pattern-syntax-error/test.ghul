namespace T is
    union Opt is
        SOME(value: int);
        NONE;
    si

    class M is
        f(o: Opt) is
            // A constructor-argument group after the ascribed type is
            // not grammatical in a `when` arm. It must be rejected with
            // a located syntax error, not fall through to expression-list
            // parsing and reach IL generation.
            case o
            when _: Opt.SOME(0) then
            when v: Opt.SOME then
            when _: Opt.NONE then
            esac
        si
    si
si
