namespace T is
    class M is
        f(o: object) is
            // A malformed `when` arm pattern: the ascription `v: )` is
            // parsed once to decide it is a pattern (not an expression),
            // then parsed again to build it. The speculative first parse
            // rolls its diagnostics back, so the error the second parse
            // raises at the same spot must still be reported: it must not
            // be taken for a repeat and dropped, which would swallow the
            // whole arm without a single diagnostic.
            case o
            when v: ) then
            esac
        si
    si
si
