namespace TestUnitVariantSingleton is
    use Std = IO.Std;

    @IL.output("il.out")
    union COLOR is
        RED;
        GREEN;
        BLUE;
    si

    class Main is
        entry() static is
            let c: COLOR = COLOR.RED();
            return;
        si
    si
si
