namespace Test is








    use Std = IO.Std;

    class Main is
        entry() static is
            
            let ex = System.Exception("should exit process");

            @IL.output("il.out")
            throw ex;
        si
    si
si