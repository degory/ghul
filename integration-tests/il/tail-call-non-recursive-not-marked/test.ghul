namespace Test is
    use Std = IO.Std;

    class MAIN is
        helper() -> int is
            return 42;
        si

        wrapper() -> int is
            @IL.output("il.out")
            return helper();
        si

        entry() static is
        si
    si
si
