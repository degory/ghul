namespace Test is
    class Main is
        test() is
            let b: bool mut;
            let x: ushort mut , y: ushort, z: ushort;

            @IL.output("il.out")
            x = y + z;

            @IL.output("il.out")
            x = y - z;

            @IL.output("il.out")
            x = y * z;

            @IL.output("il.out")
            x = y / z;

            @IL.output("il.out")
            b = y < z;

            @IL.output("il.out")
            b = y <= z;

            @IL.output("il.out")
            b = y > z;

            @IL.output("il.out")
            b = y >= z;

            @IL.output("il.out")
            b = y == z;

            @IL.output("il.out")
            b = y != z;
 
            @IL.output("il.out")
            x = \y;

            @IL.output("il.out")
            x = y << z;

            @IL.output("il.out")
            x = y >> z;

            @IL.output("il.out")
            x = y & z;

            @IL.output("il.out")
            x = y | z;
        si
    si
si