namespace Test is
    use Std = IO.Std;

    count_down(n: int ref) -> int is
        if n! == 0 then
            return 0;
        fi

        let m = n! - 1;

        @IL.output("il.out")
        return count_down(m ref);
    si

    class MAIN is
        entry() static is
        si
    si
si
