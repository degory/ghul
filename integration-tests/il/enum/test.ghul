namespace Test is
    use Std = IO.Std;

    @IL.output("il.out")
    enum Blah is
        BLOOP,
        BLIP,
        BLERGH,
        BLOGGLE = 40,
        BLANGER = 50
    si
    
    class Main is
        entry() static is
            

            return;
        si
    si
si