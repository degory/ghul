namespace TestClassImplementTraits is
    use Std = IO.Std;

    trait BaseA is
    si

    trait BaseB is        
    si
    
    @IL.output("il.out")
    class Derived: BaseA, BaseB is  
    si    
si