namespace Test is
    class BASE[T]: Collections.Iterable[T] is
        init() is si

        iterator: Collections.Iterator[T] => throw System.Exception("not reached");
    si

    class DERIVED: BASE[int] is
        init() is super.init(); si
    si
si
