namespace Test is








    use Std = IO.Std;

    class BaseA is
    si

    trait BaseB is        
    si

    trait BaseC is        
    si    
    
    @IL.output("il.out")
    class Derived: BaseA, BaseB, BaseC is
    si
si  