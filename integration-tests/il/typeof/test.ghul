namespace Test is










    use Std = IO.Std;

    use TYPE = System.Type2;
    
    class Main is
        entry() static is
            

            let ti: TYPE mut;

            @IL.output("il.out")
            ti = typeof int;

            let ts: TYPE mut;

            @IL.output("il.out")
            ts = typeof string;

            let tt: TYPE mut;

            @IL.output("il.out")
            tt = typeof (int,int,int);
        si
    si
si