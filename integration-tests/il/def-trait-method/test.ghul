namespace Test is








    use Std = IO.Std;

    trait Main is
        @IL.output("il.out")
        do_something();

        @IL.output("il.out")
        do_a_different_thing(i: int, s: string);
    si
si