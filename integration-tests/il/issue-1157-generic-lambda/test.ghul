namespace Test is
    use Std = IO.Std;

    test[T](t: T) is
        let write_t = () is
            @IL.output("il.out")
            let list = [t, t, t];
            Std.out.write_line("t is {t}");
        si;
        write_t();
    si

    class Main is
        entry() static is
            test[int](123);
            return;
        si
    si
si
