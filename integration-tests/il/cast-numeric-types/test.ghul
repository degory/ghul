namespace Test is








    use Std = IO.Std;

    class Main is
        entry() static is
            

            let s = cast short(12345);
            let i = 12345;
            let l = 12345L;

            @IL.output("il.out")
            cast bool(i);

            @IL.output("il.out")
            cast ubyte(i);
            @IL.output("il.out")
            cast byte(i);

            @IL.output("il.out")
            cast char(i);

            @IL.output("il.out")
            cast ushort(i);
            @IL.output("il.out")
            cast short(i);

            @IL.output("il.out")
            cast uint(i);
            @IL.output("il.out")
            cast int(i);

            @IL.output("il.out")
            cast ulong(i);
            @IL.output("il.out")
            cast long(i);
        si
    si
si