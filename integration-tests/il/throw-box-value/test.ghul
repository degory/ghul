namespace Test is








    use Std = IO.Std;

    class Main is
        entry() static is
            

            @IL.output("il.out")
            throw 12345;

            @IL.output("il.out")
            throw "hello world";

            @IL.output("il.out")
            throw System.Exception();
        si
    si
si