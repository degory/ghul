@IL.output("il.out")
namespace Namespace is
    use Std = IO.Std;
    
    class CLASS is
        init() is
        si        

        @IL.name("Main")
        entry() static is
            let instance: CLASS mut;
            instance = CLASS();

            instance.test("Hello World!");
        si

        test(text: string) is
        si        
    si
si
