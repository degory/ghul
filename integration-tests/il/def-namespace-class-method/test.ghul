@IL.output("il.out")
namespace Namespace is
    use Std = IO.Std;
    
    class Class is
        init() is
        si        

        @IL.name("Main")
        entry() static is
            let instance: Class;
            instance = Class();

            instance.test("Hello World!");
        si

        test(text: string) is
        si        
    si
si
