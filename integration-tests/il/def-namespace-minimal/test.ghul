// Note: actually expect no output here - due to weird (buggy?) behaviour in IL asm, need to output .namespace for each
// definition individually with fully qualified name as nesting .namespace does not work reliably  
@IL.output("il.out")
namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

si