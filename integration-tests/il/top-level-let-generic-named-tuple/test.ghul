// A top-level let whose named tuple type comes from a generic call keeps
// its element names on the global field, as one written out does.
unit[T](value: T) -> (value: T, log: string) => (value = value, log = "note")

let inferred = unit(5)
let written: (value: int, log: string) = unit(5)
