@IL.output("il.out")
namespace Test is
    // Abstract class: emits the `abstract` IL flag on the .class.
    class Animal abstract is si

    // Concrete class: no `abstract` flag.
    class Cat is si
si
