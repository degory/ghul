namespace Test is
    use Std = IO.Std;

    class Generic[T] is
        value: T;

        init(v: T) is
            self.value = v;
        si

        method() is
            let write_value = () is
                @IL.output("il.out")
                Std.out.write_line("value is {self.value}");
            si;
            write_value();
        si
    si

    class Main is
        entry() static is
            Generic[int](42).method();
            return;
        si
    si
si
