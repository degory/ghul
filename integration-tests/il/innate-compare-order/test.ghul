namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class Main is
        entry() static is
            let b: bool;

            @IL.output("il.out")
            b = 100 < 200;
            @IL.output("il.out")
            b = 100 > 200;
            @IL.output("il.out")
            b = 100 <= 100;
            @IL.output("il.out")
            b = 100 >= 201;
            @IL.output("il.out")
            b = 100L > 200L;
            @IL.output("il.out")
            b = 200L < 150L;
            @IL.output("il.out")
            b = 150L >= 50L;
            @IL.output("il.out")
            b = 200L <= 100L;
        si
    si
si