namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class Main is
        @IL.output("il.out")
        test(
            bool_: bool,
            char_: char,
            byte_: byte,
            ubyte_: ubyte,
            short_: short,
            ushort_: ushort,
            int_: int,
            uint_: uint,
            long_: long,
            ulong_: ulong,
            word_: word,
            uword_: uword
        )
        is
        si
    si
si