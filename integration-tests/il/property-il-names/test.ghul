namespace Test is








    use Std = IO.Std;

    class Main is
        @IL.output("il.out")
        @IL.name("Thing")
        thing: int => 123, = value is si

        entry() static is
            

            return;
        si
    si

    class Thing[T] is
        @IL.output("il.out")
        @IL.name("Things")
        things: Collections.Iterable[T] => null, = value is si
    si
    
si