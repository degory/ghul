namespace Test is








    use Std = IO.Std;

    
    class Main is
        entry() static is
            

            let index = 0;

            @IL.output("il.out")
            while index < 10 do
                index = index + 1;
            od
            
            return;
        si
    si
si
