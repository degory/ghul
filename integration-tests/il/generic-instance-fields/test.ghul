namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class TEST[T,U] is
        _t: T;
        _u: U;

        init(t: T, u: U) is
            @IL.output("il.out")
            _t = t;
            @IL.output("il.out")
            _u = u;
        si

        get_t() -> T is
            @IL.output("il.out")
            return _t;
        si

        get_u() -> U is
            @IL.output("il.out")
            return _u;            
        si
    si
si