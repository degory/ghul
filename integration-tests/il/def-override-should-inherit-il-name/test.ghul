namespace Test is








    use Std = IO.Std;

    
    class Main is
        entry() static is
            

            return;
        si
    si

    class BASE is
        @IL.output("il.out")
        @IL.name("ActuallyTest")
        test() is
            
        si

        @IL.name("ActuallyOther")
        other(s: string) is            
        si
    si

    class DERIVED: BASE is
        @IL.output("il.out")
        test() is
            
        si

        other(s: string) is            
        si
        
        @IL.output("il.out")
        test(i: int) is            
        si
    si
si