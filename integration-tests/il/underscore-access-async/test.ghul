@IL.output("il.out")
namespace Test is
    class A is
        _compute() -> Tasks.TASK[int] is
            let x = await Tasks.TASK.from_result(42);
            return x;
        si

        run() -> Tasks.TASK[int] is
            let y = await _compute();
            return y;
        si
    si
si
