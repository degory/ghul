
namespace Tests is
    use IO.Std.write_line;

    class StructGetType is
        entry() static is
            

            @IL.output("il.out")
            'c' .get_type();

            @IL.output("il.out")
            123 .get_type();

            @IL.output("il.out")
            123.456D . get_type();

            @IL.output("il.out")
            TEST().get_type();
        si
    si

    struct TEST is
        init() is si
    si    
si

