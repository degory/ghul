namespace Test is
    class Main is
        test() is
            let b: bool;
            let x: byte, y: byte, z: byte;

            @IL.output("il.out")
            x = -y;

            @IL.output("il.out")
            x = y + z;

            @IL.output("il.out")
            x = y - z;

            @IL.output("il.out")
            x = y * z;

            @IL.output("il.out")
            x = y / z;

            @IL.output("il.out")
            b = y < z;

            @IL.output("il.out")
            b = y <= z;

            @IL.output("il.out")
            b = y > z;

            @IL.output("il.out")
            b = y >= z;

            @IL.output("il.out")
            b = y == z;

            @IL.output("il.out")
            b = y != z;
 
            @IL.output("il.out")
            x = \y;

            @IL.output("il.out")
            x = y << z;

            @IL.output("il.out")
            x = y >> z;

            @IL.output("il.out")
            x = y ∩ z;

            @IL.output("il.out")
            x = y ∪ z;
        si
    si
si