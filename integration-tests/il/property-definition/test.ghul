namespace Test is








    use Std = IO.Std;

    class Main is
        @IL.output("il.out")
        thing: int => 123, = value is si

        entry() static is
            

            return;
        si
    si

    class Thing[T] is
        @IL.output("il.out")
        things: Collections.Iterable[T] => null, = value is si
    si
si