use IO.Std.write_line;

entry() is
    let x = (let y = 5; y * 2);
    write_line("x = {x}");

    let z = (let a = 3; let b = 4; a + b);
    write_line("z = {z}");

    let n = (write_line("side effect"); 42);
    write_line("n = {n}");
si
