namespace Test is
    // Tests: the right-hand side of `|>` must be a function call. A bare
    // value is a parse-time error.

    class MAIN is
        entry() static is
            let x = 3;
            let r = 5 |> x;
        si
    si
si