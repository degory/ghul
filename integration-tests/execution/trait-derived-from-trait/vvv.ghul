namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class Main is
        entry() static is
            

            let f = new FFFF();
            let g = new GGGG();

            Std.out.write_line("FFFF isa Base: " + isa Base(f));
            Std.out.write_line("FFFF isa SettableBase: " + isa SettableBase(f));

            Std.out.write_line("GGGG isa Base: " + isa Base(g));
        si            
    si    
si
