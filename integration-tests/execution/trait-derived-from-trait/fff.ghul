namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    class FFFF: SettableBase is
        some_field: int public;

        init() is si
    si

    class GGGG: Base is
        some_field: int public;

        init() is si
    si
si
