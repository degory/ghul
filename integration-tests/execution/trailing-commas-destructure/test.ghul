namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            let t = (1, 2, 3);

            // Destructuring left-hand side with trailing comma.
            let (a, b, c,) = t;

            Std.write_line("{a} {b} {c}");
            return;
        si
    si
si
