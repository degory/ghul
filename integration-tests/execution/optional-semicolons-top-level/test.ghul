use IO.Std.write_line
use Ghul.Pipes

let words = ["plough", "sword"]

words |> each(word => write_line(word))

let countdown = (n: int) rec is
    write_line("{n}")
    if n > 0 then
        let next = n - 1
        rec(next)
    fi
si

countdown(2)
