namespace Other is
    class Utility is
        greet() -> string static => "hello from Utility";
    si
si

namespace Test is
    use IO.Std.write_line;
    use Other.*;

    entry() is
        write_line("{Utility.greet()}");
    si
si
