use default
use Ghul.apply

// The literal's destructured parameter has no type on the first walk:
// the pack it takes is bound by the later call. The destructure waits
// for that walk rather than reporting, and types its leaves from the
// settled tuple.
let g = ((a, b)) => a + b

write_line("{apply(g, 1, 2)}")
