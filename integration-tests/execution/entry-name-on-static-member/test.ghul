// The entry-point name selects a global function; a static member that
// shares it is an ordinary member, as it is for the environment
// parameter, so its shape is nobody's business.
entry() is
    IO.Std.write_line("started at entry")
si

class LOG is
    init() is si

    entry(n: int) -> string static => "{n}"
si
