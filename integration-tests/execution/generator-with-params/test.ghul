namespace Test is
    use Std = IO.Std;

    class Main is
        range(start: int, finish: int) -> Collections.Iterable[int] static is
            yield start;
            yield finish;
            yield start + finish;
        si

        entry() static is
            for x in range(3, 7) do
                Std.write_line("r: {x}");
            od
        si
    si
si
