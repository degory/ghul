namespace Test is

    use Std = IO.Std;

    use TYPE = System.Type;

    class Main is
        entry() static is
            let t: TYPE = typeof int;
            Std.out.write_line("System.Type holds: {t}");
        si
    si
si
