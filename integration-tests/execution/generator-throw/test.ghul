namespace Test is
    use Std = IO.Std;

    class Main is
        gen() -> Ghul.Pipes.Pipe[int] static is
            yield 1;
            throw System.InvalidOperationException("boom");
        si

        entry() static is
            try
                for x in gen() do Std.write_line("got: {x}"); od
            catch ex: System.InvalidOperationException
                Std.write_line("caught {ex}");
            yrt
        si
    si
si
