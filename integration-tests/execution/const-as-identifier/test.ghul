namespace Test is
    use IO.Std;

    entry() is
        let const = 42;

        Std.write_line("const = {const}");

        Std.write_line(label("hello"));
    si

    label(const: string) -> string => "label: {const}";
si
