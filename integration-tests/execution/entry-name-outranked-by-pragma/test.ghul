// A pragma names the entry point over any name match, so the program
// never starts at the function called `entry` and its shape is not
// consulted.
entry(point: int) -> string => "{point}"

@entry()
real_start() is
    IO.Std.write_line("started at real_start")
si
