namespace Test is
    use IO.Std;
    use Collections.LIST;

    class Main is
        entry() static is
            let xs: LIST[int] mut;
            xs = new();
            xs.add(7);
            xs.add(11);
            Std.write_line("count={xs.count}");
            return;
        si
    si
si
