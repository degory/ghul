namespace Test is
    use Std = IO.Std;
    class Main is
        compute(seed: int) -> Tasks.TASK[int] static is
            let x = seed + await Tasks.TASK.from_result(42);
            return x;
        si
        entry() static is
            Std.write_line("got={compute(7).result}");
        si
    si
si
