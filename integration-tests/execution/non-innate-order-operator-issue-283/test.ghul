namespace Test is
    use IO.Std;

    class Box is
        value: int;
        init(value: int) is
            self.value = value;
        si
        <>(other: Box) -> int => value - other.value;
    si

    class Main is
        entry() static is
            let a = Box(5);
            let b = Box(10);
            Std.write_line("a > b should be False: {a > b}");
            Std.write_line("b > a should be True: {b > a}");
            Std.write_line("a < b should be True: {a < b}");
            Std.write_line("a >= b should be False: {a >= b}");
            Std.write_line("b >= a should be True: {b >= a}");
            return;
        si
    si
si
