namespace Test.PublicFields is
    use Collections;

    class TEST is
        value: int field;

        init(f: int) is
            value = f;
        si
    si

    entry() is
        let t = TEST(5);

        IO.Std.write_line("field value is {t.value}");
    si    
si
