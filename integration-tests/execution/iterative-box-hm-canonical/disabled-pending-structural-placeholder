Iterative-inference probe: Hindley-Milner canonical case where a
generic class's type arg is unbound at construction and must be
inferred from later usage.

Fails today, with two distinct issues:

1. **Compiler crash** (likely pre-existing, not caused by the
   iterative-inference work): `m.set(42)` triggers an NRE in
   Symbols.Function.try_bind_generic_arguments →
   GENERIC_ARGUMENT_BIND_RESULTS.check_complete. None of the
   iterative-inference commits touch that binding path. Worth
   filing as its own bug; iterative work should land against a
   stable baseline.

2. **Structural placeholder gap**: even with the crash fixed,
   today's INFERRED_VARIABLE_TYPE only carries
   origin: Symbols.Variable. A `Box()` constructor with no LHS
   constraint and no args leaves T structurally unbound — the
   placeholder belongs to a type-arg slot, not a Variable. Need
   to extend the placeholder mechanism (or add a sibling
   placeholder type) with origin pointing at the constructor's
   AST node, then back-feed at the constructor-call site once
   downstream usage pins the type-arg.

Re-enable (delete this file) once both issues are addressed.
