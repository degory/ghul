class Main is    
    entry() static is
        

        IO.Std.write_line("global function: {0}", global_function(123));

        return;
    si
si


global_function(i: Ghul.Intrinsics.int) -> int => i * 2;
