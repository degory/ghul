use IO.Std.write_line

// An index whose range ends in a member access is a range, not a type
// argument carrying an argument-pack marker.

let text = "multisplit"
let start = 5

write_line(text[start..text.length])
write_line(text[0..start])
