// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    typed-literal
// position: return-spine-depth-2-named-maker
// pinning:  unpinned
// arity:    8

namespace Grid is
    use IO.Std.write_line;

    make_2(j: int) -> (int) -> (int, int, int, int, int, int, int, int) -> int => k => (a: int, b: int, c: int, d: int, e: int, f: int, g: int, h: int) -> int => a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000 + h * 10000000;

    once_2[T.., U](make: (int) -> (int) -> T.. -> U) -> T.. -> U => v => make(10)(20)(v);

    cell() -> int is
        let g = once_2(make_2);
        return g(1, 2, 3, 4, 5, 6, 7, 8);
    si

    entry() is
        try
            let result = cell();

            if result == 87654321 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 87654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
