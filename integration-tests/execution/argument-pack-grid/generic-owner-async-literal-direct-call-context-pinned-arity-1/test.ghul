// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   generic-owner
// shape:    async-literal
// position: direct-call
// pinning:  context-pinned
// arity:    1

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS[T.., U] is
        init() is si

        once(f: T.. -> U) -> T.. -> U => v => f(v);
    si

    cell() -> int is
        let combinators = COMBINATORS[int, Tasks.TASK[int]]();
        let g: (int) -> Tasks.TASK[int] = combinators.once(a is await Tasks.TASK.yield(); return a; si);
        return (g(1)).result;
    si

    entry() is
        try
            let result = cell();

            if result == 1 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 1");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
