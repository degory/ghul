// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    async-literal
// position: return-spine-depth-2
// pinning:  sibling-pinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    run_2[T.., U](make: (int) -> (int) -> T.. -> U, v: T..) -> U => make(10)(20)(v);

    cell() -> int is
        return (run_2(j => k => (a, b) is await Tasks.TASK.yield(); return a + b * 10; si, 1, 2)).result;
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
