// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    async-literal
// position: return-spine-depth-2-named-maker
// pinning:  sibling-pinned
// arity:    0

namespace Grid is
    use IO.Std.write_line;

    make_2(j: int) -> (int) -> () -> Tasks.TASK[int] => k => () is await Tasks.TASK.yield(); return 42; si;

    run_2[T.., U](make: (int) -> (int) -> T.. -> U, v: T..) -> U => make(10)(20)(v);

    cell() -> int is
        return (run_2(make_2)).result;
    si

    entry() is
        try
            let result = cell();

            if result == 42 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 42");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
