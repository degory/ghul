// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    block-literal
// position: operator
// pinning:  sibling-pinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    <*>[T.., U](f: T.. -> U, v: T) -> U => f(v);

    cell() -> int is
        return ((a, b) is let t = a + b * 10; t si) <*> (1, 2);
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
