// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   static-method
// shape:    stored-value
// position: direct-call
// pinning:  context-pinned
// arity:    0

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS is

        once[T.., U](f: T.. -> U) -> T.. -> U static => v => f(v);
    si

    cell() -> int is
        let stored = () -> int => 42;
        let g: () -> int = COMBINATORS.once(stored);
        return g();
    si

    entry() is
        try
            let result = cell();

            if result == 42 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 42");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
