// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    stored-value
// position: extra-argument
// pinning:  context-pinned
// arity:    7

namespace Grid is
    use IO.Std.write_line;

    retry_n[T.., U](f: T.. -> U, attempts: int) -> T.. -> U => v => f(v);

    cell() -> int is
        let stored = (a: int, b: int, c: int, d: int, e: int, f: int, g: int) -> int => a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000;
        let g: (int, int, int, int, int, int, int) -> int = retry_n(stored, 3);
        return g(1, 2, 3, 4, 5, 6, 7);
    si

    entry() is
        try
            let result = cell();

            if result == 7654321 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 7654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
