// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    block-literal
// position: pipe
// pinning:  unpinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    once[T.., U](f: T.. -> U) -> T.. -> U => v => f(v);

    cell() -> int is
        let g = ((a, b) is let t = a + b * 10; t si) |> once();
        return g(1, 2);
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
