// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    typed-literal
// position: pipe
// pinning:  unpinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    once[T.., U](f: T.. -> U) -> T.. -> U => v => f(v);

    cell() -> int is
        let g = ((a: int, b: int) -> int => a + b * 10) |> once();
        return g(1, 2);
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
