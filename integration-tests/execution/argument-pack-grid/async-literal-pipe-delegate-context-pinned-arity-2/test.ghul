// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    async-literal
// position: pipe-delegate
// pinning:  context-pinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    once_d[T.., U](f: System.Converter[T, U]) -> T.. -> U => v => f.invoke(v);

    cell() -> int is
        let g: (int, int) -> Tasks.TASK[int] = ((a, b) is await Tasks.TASK.yield(); return a + b * 10; si) |> once_d();
        return (g(1, 2)).result;
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
