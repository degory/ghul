// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    async-literal
// position: return-spine-depth-1-stored-maker
// pinning:  sibling-pinned
// arity:    7

namespace Grid is
    use IO.Std.write_line;

    run_1[T.., U](make: (int) -> T.. -> U, v: T..) -> U => make(10)(v);

    cell() -> int is
        let make = (k: int) => (a, b, c, d, e, f, g) is await Tasks.TASK.yield(); return a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000; si;
        return (run_1(make, 1, 2, 3, 4, 5, 6, 7)).result;
    si

    entry() is
        try
            let result = cell();

            if result == 7654321 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 7654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
