// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    typed-literal
// position: return-spine-depth-2-declared-return
// pinning:  unpinned
// arity:    1

namespace Grid is
    use IO.Std.write_line;

    once_2[T.., U](make: (int) -> (int) -> T.. -> U) -> T.. -> U => v => make(10)(20)(v);

    cell() -> int is
        let g = once_2((j: int) -> (int) -> (int) -> int => (k: int) -> (int) -> int => (a: int) -> int => a);
        return g(1);
    si

    entry() is
        try
            let result = cell();

            if result == 1 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 1");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
