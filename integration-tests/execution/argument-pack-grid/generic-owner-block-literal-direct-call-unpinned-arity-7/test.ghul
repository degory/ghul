// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   generic-owner
// shape:    block-literal
// position: direct-call
// pinning:  unpinned
// arity:    7

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS[T.., U] is
        init() is si

        once(f: T.. -> U) -> T.. -> U => v => f(v);
    si

    cell() -> int is
        let combinators = COMBINATORS[(int, int, int, int, int, int, int), int]();
        let g = combinators.once((a, b, c, d, e, f, g) is let t = a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000; t si);
        return g(1, 2, 3, 4, 5, 6, 7);
    si

    entry() is
        try
            let result = cell();

            if result == 7654321 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 7654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
