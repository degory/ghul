// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    async-literal
// position: operator
// pinning:  sibling-pinned
// arity:    1

namespace Grid is
    use IO.Std.write_line;

    <*>[T.., U](f: T.. -> U, v: T) -> U => f(v);

    cell() -> int is
        return ((a is await Tasks.TASK.yield(); return a; si) <*> 1).result;
    si

    entry() is
        try
            let result = cell();

            if result == 1 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 1");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
