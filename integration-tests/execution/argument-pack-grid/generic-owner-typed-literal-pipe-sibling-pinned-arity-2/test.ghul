// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   generic-owner
// shape:    typed-literal
// position: pipe
// pinning:  sibling-pinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS[T.., U] is
        init() is si

        apply_n(f: T.. -> U, v: T..) -> U => f(v);
    si

    cell() -> int is
        let combinators = COMBINATORS[(int, int), int]();
        return ((a: int, b: int) -> int => a + b * 10) |> combinators.apply_n(1, 2);
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
