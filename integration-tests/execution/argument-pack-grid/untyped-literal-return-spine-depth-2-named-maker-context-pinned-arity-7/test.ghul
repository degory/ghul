// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    untyped-literal
// position: return-spine-depth-2-named-maker
// pinning:  context-pinned
// arity:    7

namespace Grid is
    use IO.Std.write_line;

    make_2(j: int) -> (int) -> (int, int, int, int, int, int, int) -> int => k => (a, b, c, d, e, f, g) => a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000;

    once_2[T.., U](make: (int) -> (int) -> T.. -> U) -> T.. -> U => v => make(10)(20)(v);

    cell() -> int is
        let g: (int, int, int, int, int, int, int) -> int = once_2(make_2);
        return g(1, 2, 3, 4, 5, 6, 7);
    si

    entry() is
        try
            let result = cell();

            if result == 7654321 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 7654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
