// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    async-literal
// position: return-spine-depth-2-named-maker
// pinning:  unpinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    make_2(j: int) -> (int) -> (int, int) -> Tasks.TASK[int] => k => (a, b) is await Tasks.TASK.yield(); return a + b * 10; si;

    once_2[T.., U](make: (int) -> (int) -> T.. -> U) -> T.. -> U => v => make(10)(20)(v);

    cell() -> int is
        let g = once_2(make_2);
        return (g(1, 2)).result;
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
