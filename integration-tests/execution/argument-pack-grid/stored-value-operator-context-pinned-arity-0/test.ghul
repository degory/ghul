// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    stored-value
// position: operator
// pinning:  context-pinned
// arity:    0

namespace Grid is
    use IO.Std.write_line;

    >=>[T.., U, V](f: T.. -> U, g: (U) -> V) -> T.. -> V => v => g(f(v));

    inc(n: int) -> int => n + 1;

    cell() -> int is
        let stored = () -> int => 42;
        let g: () -> int = stored >=> inc;
        return g();
    si

    entry() is
        try
            let result = cell();

            if result == 43 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 43");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
