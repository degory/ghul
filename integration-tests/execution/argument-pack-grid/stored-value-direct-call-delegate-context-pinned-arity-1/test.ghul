// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    stored-value
// position: direct-call-delegate
// pinning:  context-pinned
// arity:    1

namespace Grid is
    use IO.Std.write_line;

    once_d[T.., U](f: System.Converter[T, U]) -> T.. -> U => v => f.invoke(v);

    cell() -> int is
        let stored = (a: int) -> int => a;
        let g: (int) -> int = once_d(stored);
        return g(1);
    si

    entry() is
        try
            let result = cell();

            if result == 1 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 1");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
