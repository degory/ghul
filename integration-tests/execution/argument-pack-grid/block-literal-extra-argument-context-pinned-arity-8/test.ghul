// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    block-literal
// position: extra-argument
// pinning:  context-pinned
// arity:    8

namespace Grid is
    use IO.Std.write_line;

    retry_n[T.., U](f: T.. -> U, attempts: int) -> T.. -> U => v => f(v);

    cell() -> int is
        let g: (int, int, int, int, int, int, int, int) -> int = retry_n((a, b, c, d, e, f, g, h) is let t = a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000 + h * 10000000; t si, 3);
        return g(1, 2, 3, 4, 5, 6, 7, 8);
    si

    entry() is
        try
            let result = cell();

            if result == 87654321 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 87654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
