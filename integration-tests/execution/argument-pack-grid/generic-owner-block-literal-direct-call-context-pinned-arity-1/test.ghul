// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   generic-owner
// shape:    block-literal
// position: direct-call
// pinning:  context-pinned
// arity:    1

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS[T.., U] is
        init() is si

        once(f: T.. -> U) -> T.. -> U => v => f(v);
    si

    cell() -> int is
        let combinators = COMBINATORS[int, int]();
        let g: (int) -> int = combinators.once(a is let t = a; t si);
        return g(1);
    si

    entry() is
        try
            let result = cell();

            if result == 1 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 1");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
