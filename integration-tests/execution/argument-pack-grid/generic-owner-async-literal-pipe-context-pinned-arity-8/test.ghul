// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   generic-owner
// shape:    async-literal
// position: pipe
// pinning:  context-pinned
// arity:    8

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS[T.., U] is
        init() is si

        once(f: T.. -> U) -> T.. -> U => v => f(v);
    si

    cell() -> int is
        let combinators = COMBINATORS[(int, int, int, int, int, int, int, int), Tasks.TASK[int]]();
        let g: (int, int, int, int, int, int, int, int) -> Tasks.TASK[int] = ((a, b, c, d, e, f, g, h) is await Tasks.TASK.yield(); return a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000 + h * 10000000; si) |> combinators.once();
        return (g(1, 2, 3, 4, 5, 6, 7, 8)).result;
    si

    entry() is
        try
            let result = cell();

            if result == 87654321 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 87654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
