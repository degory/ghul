// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    untyped-literal
// position: return-spine-depth-2-declared-return
// pinning:  sibling-pinned
// arity:    7

namespace Grid is
    use IO.Std.write_line;

    run_2[T.., U](make: (int) -> (int) -> T.. -> U, v: T..) -> U => make(10)(20)(v);

    cell() -> int is
        return run_2((j: int) -> (int) -> (int, int, int, int, int, int, int) -> int => (k: int) -> (int, int, int, int, int, int, int) -> int => (a, b, c, d, e, f, g) => a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000, 1, 2, 3, 4, 5, 6, 7);
    si

    entry() is
        try
            let result = cell();

            if result == 7654321 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 7654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
