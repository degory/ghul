// Generated by integration-tests/generators/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    named-function
// position: return-spine-depth-2
// pinning:  context-pinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    weigh(a: int, b: int) -> int => a + b * 10;

    once_2[T.., U](make: (int) -> (int) -> T.. -> U) -> T.. -> U => v => make(10)(20)(v);

    cell() -> int is
        let g: (int, int) -> int = once_2(j => k => weigh);
        return g(1, 2);
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
