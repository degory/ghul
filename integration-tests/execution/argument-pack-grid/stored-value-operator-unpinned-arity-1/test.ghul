// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    stored-value
// position: operator
// pinning:  unpinned
// arity:    1

namespace Grid is
    use IO.Std.write_line;

    >=>[T.., U, V](f: T.. -> U, g: (U) -> V) -> T.. -> V => v => g(f(v));

    inc(n: int) -> int => n + 1;

    cell() -> int is
        let stored = (a: int) -> int => a;
        let g = stored >=> inc;
        return g(1);
    si

    entry() is
        try
            let result = cell();

            if result == 2 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 2");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
