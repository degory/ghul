// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    named-function
// position: return-spine-depth-2-named-maker
// pinning:  context-pinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    weigh(a: int, b: int) -> int => a + b * 10;

    make_2(j: int) -> (int) -> (int, int) -> int => k => weigh;

    once_2[T.., U](make: (int) -> (int) -> T.. -> U) -> T.. -> U => v => make(10)(20)(v);

    cell() -> int is
        let g: (int, int) -> int = once_2(make_2);
        return g(1, 2);
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
