// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    block-literal
// position: return-spine-depth-1
// pinning:  context-pinned
// arity:    0

namespace Grid is
    use IO.Std.write_line;

    once_1[T.., U](make: (int) -> T.. -> U) -> T.. -> U => v => make(10)(v);

    cell() -> int is
        let g: () -> int = once_1(k => () is let t = 42; t si);
        return g();
    si

    entry() is
        try
            let result = cell();

            if result == 42 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 42");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
