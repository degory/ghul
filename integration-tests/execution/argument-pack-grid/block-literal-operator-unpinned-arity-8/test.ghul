// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    block-literal
// position: operator
// pinning:  unpinned
// arity:    8

namespace Grid is
    use IO.Std.write_line;

    >=>[T.., U, V](f: T.. -> U, g: (U) -> V) -> T.. -> V => v => g(f(v));

    inc(n: int) -> int => n + 1;

    cell() -> int is
        let g = ((a, b, c, d, e, f, g, h) is let t = a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000 + h * 10000000; t si) >=> inc;
        return g(1, 2, 3, 4, 5, 6, 7, 8);
    si

    entry() is
        try
            let result = cell();

            if result == 87654322 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 87654322");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
