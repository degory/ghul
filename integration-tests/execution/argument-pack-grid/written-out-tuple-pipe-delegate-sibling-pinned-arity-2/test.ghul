// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    written-out-tuple
// position: pipe-delegate
// pinning:  sibling-pinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    weigh(a: int, b: int) -> int => a + b * 10;

    apply_d[T.., U](f: System.Converter[T, U], v: T..) -> U => f.invoke(v);

    cell() -> int is
        return weigh |> apply_d((1, 2));
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
