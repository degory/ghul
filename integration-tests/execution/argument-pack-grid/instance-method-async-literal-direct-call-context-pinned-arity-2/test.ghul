// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   instance-method
// shape:    async-literal
// position: direct-call
// pinning:  context-pinned
// arity:    2

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS is
        init() is si

        once[T.., U](f: T.. -> U) -> T.. -> U => v => f(v);
    si

    cell() -> int is
        let combinators = COMBINATORS();
        let g: (int, int) -> Tasks.TASK[int] = combinators.once((a, b) is await Tasks.TASK.yield(); return a + b * 10; si);
        return (g(1, 2)).result;
    si

    entry() is
        try
            let result = cell();

            if result == 21 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 21");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
