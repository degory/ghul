// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    untyped-literal
// position: return-spine-depth-1-named-maker
// pinning:  unpinned
// arity:    8

namespace Grid is
    use IO.Std.write_line;

    make_1(k: int) -> (int, int, int, int, int, int, int, int) -> int => (a, b, c, d, e, f, g, h) => a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000 + h * 10000000;

    once_1[T.., U](make: (int) -> T.. -> U) -> T.. -> U => v => make(10)(v);

    cell() -> int is
        let g = once_1(make_1);
        return g(1, 2, 3, 4, 5, 6, 7, 8);
    si

    entry() is
        try
            let result = cell();

            if result == 87654321 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 87654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
