// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   self-call
// shape:    typed-literal
// position: pipe
// pinning:  sibling-pinned
// arity:    1

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS is
        init() is si

        apply_n[T.., U](f: T.. -> U, v: T..) -> U => f(v);

        run() -> int is
            return ((a: int) -> int => a) |> self.apply_n(1);
        si
    si

    cell() -> int is
        let combinators = COMBINATORS();
        return combinators.run();
    si

    entry() is
        try
            let result = cell();

            if result == 1 then
                write_line("ok {result}");
            else
                write_line("wrong result, expected 1");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
