// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   static-method
// shape:    untyped-literal
// position: direct-call
// pinning:  sibling-pinned
// arity:    7

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS is

        apply_n[T.., U](f: T.. -> U, v: T..) -> U static => f(v);
    si

    cell() -> int is
        return COMBINATORS.apply_n((a, b, c, d, e, f, g) => a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000, 1, 2, 3, 4, 5, 6, 7);
    si

    entry() is
        try
            let result = cell();

            if result == 7654321 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 7654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
