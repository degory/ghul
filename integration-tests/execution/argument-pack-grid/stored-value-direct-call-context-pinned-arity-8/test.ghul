// Generated by integration-tests/generators/argument-pack-grid: regenerate
// rather than edit.
//
// shape:    stored-value
// position: direct-call
// pinning:  context-pinned
// arity:    8

namespace Grid is
    use IO.Std.write_line;

    once[T.., U](f: T.. -> U) -> T.. -> U => v => f(v);

    cell() -> int is
        let stored = (a: int, b: int, c: int, d: int, e: int, f: int, g: int, h: int) -> int => a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000 + h * 10000000;
        let g: (int, int, int, int, int, int, int, int) -> int = once(stored);
        return g(1, 2, 3, 4, 5, 6, 7, 8);
    si

    entry() is
        try
            let result = cell();

            if result == 87654321 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 87654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
