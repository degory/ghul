// Generated by tools/argument-pack-grid: regenerate
// rather than edit.
//
// callee:   self-call
// shape:    async-literal
// position: pipe
// pinning:  context-pinned
// arity:    7

namespace Grid is
    use IO.Std.write_line;

    class COMBINATORS is
        init() is si

        once[T.., U](f: T.. -> U) -> T.. -> U => v => f(v);

        run() -> int is
            let g: (int, int, int, int, int, int, int) -> Tasks.TASK[int] = ((a, b, c, d, e, f, g) is await Tasks.TASK.yield(); return a + b * 10 + c * 100 + d * 1000 + e * 10000 + f * 100000 + g * 1000000; si) |> self.once();
            return (g(1, 2, 3, 4, 5, 6, 7)).result;
        si
    si

    cell() -> int is
        let combinators = COMBINATORS();
        return combinators.run();
    si

    entry() is
        try
            let result = cell();

            if result == 7654321 then
                write_line("ok {result}");
            else
                write_line("wrong result {result}, expected 7654321");
            fi
        catch e: System.Exception
            write_line("exception {e.get_type()}");
        yrt
    si
si
