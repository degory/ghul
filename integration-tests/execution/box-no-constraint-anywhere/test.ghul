namespace Test is
    use IO.Std;

    class BOX[T] is
        init() is si
    si

    class Main is
        entry() static is
            let m = BOX();  // T never constrained anywhere: reported, not defaulted
            Std.write_line("hello");
            return;
        si
    si
si
