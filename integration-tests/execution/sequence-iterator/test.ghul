namespace Test is








    use Std = IO.Std;

    
    class Main is
        entry() static is
            

            for s in [1, 2, 3, 4, 5] do
                if s > 1 then
                    Std.out.write(",");
                fi
                
                Std.out.write(s);
            od
            
            Std.out.write_line();
        si
    si
si