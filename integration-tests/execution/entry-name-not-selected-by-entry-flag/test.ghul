// With another name given as the entry point, a function called `entry`
// is not selected and its shape is nobody's business.
entry(point: int) -> string => "{point}"

other_start() is
    IO.Std.write_line("{entry(1)}")
si
