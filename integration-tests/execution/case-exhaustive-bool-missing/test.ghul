namespace Test is
    use IO.Std;

    class Main is
        entry() static is
            describe(true);
        si

        describe(b: bool) static is
            case b
            when true then Std.write_line("t");
            esac
        si
    si
si
