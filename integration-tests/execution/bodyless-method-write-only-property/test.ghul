class HOLDER is
    _v: int;

    init() is _v = 0; si

    value: int public, = x is _v = x; si

    read() -> int => _v;
si

entry() is
    let h = HOLDER();

    h.value = 3;

    IO.Std.write_line("{h.read()}");
si
