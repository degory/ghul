namespace Test is
    use Std = IO.Std;

    entry() is
        // `a | -b` is bitwise or with a negated right operand.
        let a = 0x0F;
        let b = 0x10;
        let c = a | -b;
        Std.out.write_line("{c}");
    si
si
