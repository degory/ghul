use default

// a file's own definition wins over the one the implicit use default
// brings in, and writing use default as well is harmless
write_line(s: string) is
    IO.Std.write_line("mine: {s}")
si

write_line("hello")
