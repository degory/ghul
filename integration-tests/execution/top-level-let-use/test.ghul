use IO.Std.write_line;

class SCOPE: Ghul.Disposable is
    init() is si
    dispose() is write_line("closing"); si
si

let use s = SCOPE();

note() -> string => "function sees the scope";

write_line("inside");
write_line(note());
