namespace Test is
    use Std = IO.Std;

    class Main is
        // The same literal written with an expression body, which
        // carries no `return` at all for the pre-walk classification
        // to read.
        entry() static is
            let awaited = () => await Tasks.TASK.from_result(20) + 2;

            Std.write_line("awaited={awaited().result}");
        si
    si
si
