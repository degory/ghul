use Std = IO.Std;
    
class MAIN is
    entry(args: string[]) static is
        MAIN().test(args);
    si

    init() is
    si

    test(args: string[]) is
        Std.out.write_line("have args");
    si
si
