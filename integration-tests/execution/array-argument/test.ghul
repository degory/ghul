use Std = IO.Std;
    
class Main is
    entry(args: string[]) static is
        new Main().test(args);
    si

    init() is
    si

    test(args: string[]) is
        Std.out.write_line("have args");
    si
si
