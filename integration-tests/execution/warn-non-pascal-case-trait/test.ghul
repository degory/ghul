namespace Test is
    use IO.Std;

    trait my_trait is
        describe() -> string => "trait";
    si

    class HOLDER: my_trait is
        init() is si
    si

    class Main is
        entry() static is
            Std.write_line(HOLDER().describe());
        si
    si
si
