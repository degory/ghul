entry() is
    let names = ["alice", "bob", "carol"];

    for name in names do
        IO.Std.write_line("hello, {name}");
    od

    let total = names | .count();
    IO.Std.write_line("count: {total}");
si
