use IO.Std.write_line;

entry() is
    let n = (
        let x = 10;
        let y = if x > 5 then x * 2 else 0 fi;
        y + 1
    );
    write_line("n = {n}");

    let s = (
        let kind = "cat";
        case kind
        when "cat"  then "feline"
        when "dog"  then "canine"
        else             "other"
        esac
    );
    write_line("s = {s}");
si
