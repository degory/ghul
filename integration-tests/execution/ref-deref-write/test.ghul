namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            let x = 1;
            write_through(x ref);
            Std.out.write_line("x = {x}");
        si

        write_through(out: int ref) static is
            out^ = 42;
        si
    si
si
