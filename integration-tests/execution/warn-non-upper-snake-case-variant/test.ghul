namespace Test is
    use IO.Std;

    union Direction is
        North;
        South;
        East;
        West;
    si

    class Main is
        entry() static is
            let d: Direction = Direction.North;
            if isa Direction.North(d) then
                Std.write_line("north");
            fi
        si
    si
si
