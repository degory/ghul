namespace Test is
    use Std = IO.Std;

    class Main is
        compute() -> Tasks.TASK[int] static is
            let a = await Tasks.TASK.from_result(10);
            let b = await Tasks.TASK.from_result(20);
            let c = await Tasks.TASK.from_result(30);
            return a + b + c;
        si

        entry() static is
            let result = compute();
            Std.write_line("result={result.result}");
        si
    si
si
