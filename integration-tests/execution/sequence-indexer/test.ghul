namespace Test is








    use Std = IO.Std;

    class Main is
        entry() static is
            

            let x = ["hello", "there", "world"];

            let h = x[0];
            let t = x[1];
            let w = x[2];

            Std.out.write_line("have: " + h + " " + t + " " + w);

            return;
        si
    si
si