use IO.Std.write_line;

entry() is
    let add = (a: int, b: int) rec => a + b;
    let max = (a: int, b: int) rec =>
        if a > b then a else b fi;

    write_line("add(2, 3) is {add(2, 3)}");
    write_line("max(7, 4) is {max(7, 4)}");
    write_line("max(4, 7) is {max(4, 7)}");
si
