namespace Test is
    use Std = IO.Std;

    class Main is
        compute() -> Tasks.TASK[int] static is
            return "not an int";
        si

        entry() static is
            compute();
        si
    si
si
