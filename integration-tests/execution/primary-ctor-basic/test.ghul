namespace Test is
    use Std = IO.Std;

    class BOX(value: int) is
        _value;

        get_value() -> int => _value;
    si

    class Main is
        entry() static is
            let b = BOX(42);
            Std.write_line("{b.get_value()}");
        si
    si
si
