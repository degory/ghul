namespace Test is
    use Std = IO.Std;
    use Collections.LIST;

    class Main is
        entry() static is
            let v3 = LIST();
            v3.add("world");
            let v5 mut;
            v5 = v3;

            Std.write_line("{v5[0]}");

            return;
        si
    si
si
