namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            let i: int = case 99 when 1 then 100 esac;
            Std.write_line("i = {i}");

            let b: bool = case 99 when 1 then true esac;
            Std.write_line("b = {b}");

            let s: string? = case 99 when 1 then "yes" esac;
            if s? then
                Std.write_line("s = {s}");
            else
                Std.write_line("s = <null>");
            fi

            return;
        si
    si
si
