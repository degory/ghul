use IO.Std.write_line;

sum_to(n: int, acc: long) -> long is
    if n == 0 then
        return acc;
    fi

    return sum_to(n - 1, acc + cast long(n));
si

entry() is
    write_line("{sum_to(2_000_000, 0L)}");
si
