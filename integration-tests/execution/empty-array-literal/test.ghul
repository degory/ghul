empty_returned() -> string[] static is
    return [];
si

entry() static is
    let a: string[] = [];
    let b: string[] = [ ];
    let c: int[] = [ ];

    IO.Std.write_line("{a.count} {b.count} {c.count}");
    IO.Std.write_line("returned {empty_returned().count}");

    return;
si
