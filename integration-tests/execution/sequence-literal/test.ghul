namespace Test is








    use Std = IO.Std;

    class Main is
        entry() static is
            

            let x = [1, 2, 3, 4, 5];

            Std.out.write_line("have sequence: " + x.count);

            return;
        si
    si
si