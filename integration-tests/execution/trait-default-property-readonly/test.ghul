namespace Test is
    use Std = IO.Std;

    trait HasName is
        name: string => "default-name";
    si

    class Greeter: HasName is
        init() is si
    si

    class Main is
        entry() static is
            let g = Greeter();
            Std.write_line(g.name);
        si
    si
si
