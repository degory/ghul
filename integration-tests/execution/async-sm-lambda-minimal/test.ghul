namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            let f = () is
                let x = await Tasks.TASK.from_result(42);
                return x;
            si;
            Std.write_line("got={f().result}");
        si
    si
si
