namespace Test is
    use IO.Std;

    class Main is
        entry() static is
            let total mut = 0;

            let bump = (n: int) is
                total = total + n;
            si;

            bump(5);
            Std.write_line("{total}");
        si
    si
si
