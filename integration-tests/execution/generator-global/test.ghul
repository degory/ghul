namespace Test is
    use Std = IO.Std;

    gen() -> Ghul.Pipes.Pipe[int] is
        yield 7;
        yield 8;
        yield 9;
    si

    class Main is
        entry() static is
            for x in gen() do
                Std.write_line("g: {x}");
            od
        si
    si
si
