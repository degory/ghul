namespace Test is
    add(x: int, y: int) -> int => x + y;

    // Tests: `|>` threads its subject in as a positional first argument,
    // so named written arguments cannot be combined with it.

    class MAIN is
        entry() static is
            let r = 5 |> add(y = 3);
        si
    si
si