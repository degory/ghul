namespace Test is
    use Std = IO.Std;

    class POINT(x: int, y: int) is
        x: int public;
        y: int public;
    si

    class Main is
        entry() static is
            let p = POINT(7, 11);
            Std.write_line("{p.x},{p.y}");
        si
    si
si
