namespace Test is








    use Std = IO.Std;

    use Collections;

    class Main: Whatever is
        entry() static is
            

            let list = LIST[int]();

            list.add(1);
            list.add(2);
            list.add(3);
            list.add(4);

            for i in list do
                Std.out.write_line("have: " + i);
            od
        si

        blah() is
            
        si        
    si
    
    trait Whatever is
        blah();
    si
si
