namespace Test is
    use IO.Std;

    class Main is
        entry() static is
            for i in 0..3 do
                let x: string mut;
                if x? then
                    Std.write_line("i={i} x set");
                else
                    Std.write_line("i={i} x not set");
                fi
                x = "hello";
            od
            return;
        si
    si
si
