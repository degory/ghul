namespace Test is








    use Std = IO.Std;

    class Main is
        entry() static is
            

            let list = new Collections.LIST[string]();

            list.add("one");
            list.add("two");

            Std.out.write_line("reached end");
        si
    si
si
