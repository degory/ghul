namespace Test is








    use Std = IO.Std;

    class Main is
        entry() static is
            

            return;
        si
    si

    class BASE is
        init() is            
        si
        
        static_method(i: int) -> int static is
            return i * 2;
        si
    si

    class DERIVED: BASE is
        init() is            
        si
        
        test_instance(i: int) -> int is
            return static_method(i);
        si

        test_static(i: int) -> int static is
            return static_method(i);
        si        
    si
si