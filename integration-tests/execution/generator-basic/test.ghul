namespace Test is
    use Std = IO.Std;

    class Main is
        gen() -> Collections.Iterable[int] static is
            yield 1;
            yield 2;
        si

        entry() static is
            for x in gen() do
                Std.write_line("got {x}");
            od
        si
    si
si
