namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            let x = 7;
            read(x ref);
        si

        read(p: int ref) static is
            let y = p^;
            Std.out.write_line("y = {y}");
        si
    si
si
