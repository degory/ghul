namespace Test is








    use Std = IO.Std;

    
    class Main is
        entry() static is
            
            let total = 0.0D;

            for i in 0..20 do
                total = total + cast double(i);

                Std.out.write_line("total is now: " + total + " average is now: " + total / cast double(i));
            od
        si
    si
si