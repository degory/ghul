namespace Test is








    use Std = IO.Std;

    
    class Main is
        entry() static is
            

            Std.out.write_line("hello" + " world");
            Std.out.write_line("hello " + 12345 + 67890);

            Std.out.write_line("goodbye " + "cruel " + "world");

            let s = "some";
            let t = "text";

            let u = s + " " + t;

            Std.out.write_line(u);
        si
    si
si

