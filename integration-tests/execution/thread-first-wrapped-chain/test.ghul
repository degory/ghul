namespace Test is
    use IO.Std.write_line;
    use Ghul.Pipes;

    entry() is
        let total =
            [1, 2, 3, 4] |>
                filter(x => x % 2 == 0) |>
                map(x => x * 10) |>
                reduce(0, (a, x) => a + x)

        write_line("{total}");
    si
si
