namespace Test is








    use Std = IO.Std;

    class Main is
        entry() static is
            

            let x = "xxx";
            let y = "yyy";
            let xx = "xxx";

            Std.out.write_line("expect true: x =~ x: " + (x =~ x));
            Std.out.write_line("expect false: x !~ x: " + (x !~ x));
            Std.out.write_line("expect false: x =~ y: " + (x =~ y));
            Std.out.write_line("expect true: x !~ y: " + (x !~ y));
            Std.out.write_line("expect true: x =~ xx: " + (x =~ xx));
            Std.out.write_line("expect false: x !~ xx: " + (x !~ xx));

            return;
        si
    si
si

