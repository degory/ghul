namespace Test is
    use Std = IO.Std;
    class Main is
        compute() -> Tasks.TASK[int] static is
            return await Tasks.TASK.from_result(99);
        si
        entry() static is
            Std.write_line("got={compute().result}");
        si
    si
si
