namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            let a: byte = cast byte(12);
            let b: byte = cast byte(10);
            let c = a ^ b;
            Std.out.write_line("{c}");
        si
    si
si
