namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            let n = 99;

            case n
            when 1 then Std.write_line("one");
            when 2 then Std.write_line("two");
            esac

            Std.write_line("done");

            return;
        si
    si
si
