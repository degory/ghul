#!/usr/bin/env ghul
@@suppress("top-level-statements-not-run")

// no use: write_line, the collections and the pipes come from the
// implicit use default
let totals = LIST[int]([3, 1, 2])

write_line(totals |> map(t => t * 10) |> join(", "))
