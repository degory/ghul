namespace Test is
    use IO.Std;

    class Main is
        bump(n: int) -> int static is
            n = n + 1;
            return n;
        si

        entry() static is
            Std.write_line("{bump(10)}");
        si
    si
si
