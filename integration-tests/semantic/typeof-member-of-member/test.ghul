namespace Test is
    use IO.Path

    class A is
        init() is si

        // a type expression inside typeof takes the members after it,
        // so this names a type Path.assembly.location rather than reading
        // a property of the Type
        name: string => typeof(Path).assembly.location
    si
si
