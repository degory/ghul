namespace Test is
    use IO.Std.write_line;

    class Base is
    si

    class DerivedA: Base is
        init() is si
    si

    class DerivedB: Base is
        init() is si
    si

    entry() is
        let need_base: Base =
            case 1
            when 1 then DerivedA()
            else "string instead"
            esac;

        let need_int: int =
            case 1
            when 1 then 10
            else "string"
            esac;

        write_line("done");
    si
si
