namespace Test is
    use IO.Std.write_line;

    class BASE is
    si

    class DERIVED_A: BASE is
        init() is si
    si

    class DERIVED_B: BASE is
        init() is si
    si

    entry() is
        let need_base: BASE =
            case 1
            when 1 then DERIVED_A()
            else "string instead"
            esac;

        let need_int: int =
            case 1
            when 1 then 10
            else "string"
            esac;

        write_line("done");
    si
si
