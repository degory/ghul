namespace Test is
    class BOX is
        init() is si
    si

    // No AsyncMethodBuilderAttribute, so not a task-like.
    f(x: int) -> BOX is
        await Tasks.TASK.yield();
        return BOX();
    si

    g(x: int) -> int is
        await Tasks.TASK.yield();
        return 1;
    si
si
