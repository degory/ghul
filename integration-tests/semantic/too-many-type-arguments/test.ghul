use default

class EVENT[T..] is
    init() is si

    raise(v: T..) is
        write_line("{v}")
    si
si

class BOX[T] is
    init() is si
si

entry() is
    let e = EVENT[int, string]()
    let b = BOX[int, string]()

    write_line("{e} {b}")
si
