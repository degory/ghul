namespace Test is
    @IF.not.v3()
    use Std = System.Console;
    @IF.not.v3()
    use object = System.Object;
    @IF.not.v3()
    use string = System.String;

    @IF.v3()
    use Std = IO.Std;

    
    class Main is
        value: int is
            let x: int;
            let y: string = "blah";
        si,
        = v is
            let x: int;
            let y: string = "blah";            
        si

        [index: int]: int is
            let x: int;
            let y: string = "blah";            
        si,
        = v is
            let x: int;
            let y: string = "blah";            
        si
        
        entry() static is
        si
    si
si