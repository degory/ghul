namespace Test is








    use Std = IO.Std;

    
    class Main is
        value: int is
            let x: int;
            let y: string = "blah";
        si,
        = v is
            let x: int;
            let y: string = "blah";            
        si

        [index: int]: int is
            let x: int;
            let y: string = "blah";            
        si,
        = v is
            let x: int;
            let y: string = "blah";            
        si
        
        entry() static is
        si
    si
si