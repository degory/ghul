namespace Test is
    use Std = IO.Std;

    trait Left is
        name: string => "left";
    si

    trait Right is
        name: string => "right";
    si

    class BOTH: Left, Right is
        init() is si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
