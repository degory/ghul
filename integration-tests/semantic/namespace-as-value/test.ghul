namespace Test is
    class Main is
        entry() static is
            // A namespace has no value form at all, and the
            // diagnostic says so rather than leaving the reader to
            // work out what was wrong with the expression.
            let x = IO;

            return;
        si
    si
si
