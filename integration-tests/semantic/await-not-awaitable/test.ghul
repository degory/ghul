namespace Test is
    class NOT_AWAITABLE is
        init() is si
    si

    class Main is
        compute() -> Tasks.TASK[int] static is
            let a = await 42;
            let b = await NOT_AWAITABLE();
            return a;
        si

        entry() static is
            let _ = compute();
        si
    si
si
