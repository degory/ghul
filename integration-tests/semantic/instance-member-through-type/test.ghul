class BOX is
    v: int public

    init(v: int) is
        self.v = v
    si

    plus(other: BOX) -> BOX pure => BOX(v + other.v)
si

entry() is
    let m = BOX.plus
    let order = int.`<>
    let same = string.`=~

    IO.Std.write_line("{m} {order} {same}")
si
