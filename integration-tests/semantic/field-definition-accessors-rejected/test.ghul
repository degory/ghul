namespace Test is
    class THING is
        // expect error: a `field` cannot declare a read body — drop `field`
        // to make this a property.
        value: int field is
            return 0;
        si
    si
si
