namespace Test is
    class BASE is
        init(x: int) is si
    si

    class CHILD: BASE is
        init(x: int) is
            super.init(x);
        si

        super(x);

        do_nothing() is si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
