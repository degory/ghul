// Variance can be declared only on a trait — the CLR permits variance
// only on interfaces. Declaring it on a class is an error.
namespace Test is
    class BOX[T: out] is
        _value: T;
        init(value: T) is _value = value; si
    si
si
