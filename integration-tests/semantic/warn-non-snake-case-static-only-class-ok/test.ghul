namespace Test is
    use IO.Std;

    class HelperBox is
        greet() static is
            Std.write_line("hello");
        si

        message: string static => "static";

        _seed: int static;
    si

    class Main is
        entry() static is
            HelperBox.greet();
            Std.write_line(HelperBox.message);
        si
    si
si
