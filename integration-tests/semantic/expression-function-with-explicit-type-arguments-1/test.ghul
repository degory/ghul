namespace Test is
    use Std = IO.Std;

    class Main is
        function[T](s: string) -> T static;
        function[T](t: T) -> T static;
        function[T]() -> T static;
    si

    entry() static is
        let b: int = Main.function `[int]();
        let c: (int, int) = Main.function `[(int, int)]((1, 2));
        let d: int = Main.function `[int]("hello");
        let e: string = Main.function `[string]("hello");
        return;
    si
si