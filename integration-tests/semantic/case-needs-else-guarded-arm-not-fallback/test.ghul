namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            describe("hi");
        si

        describe(o: object) static is
            case o
            when v: string /\ v.length > 3 then Std.write_line(v);
            esac
        si
    si
si
