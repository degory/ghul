namespace Test is
    use IO.Std;

    class Main is
        entry() static is
            @suppress("non-snake-case-member")
            let MyLocal = 42;
            Std.write_line("{MyLocal}");
        si
    si
si
