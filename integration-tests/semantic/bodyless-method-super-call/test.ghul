class BASE is
    init() is si

    contract() -> int;
si

class SUB: BASE is
    init() is super.init(); si

    contract() -> int => super.contract() + 1;
si

entry() is
    IO.Std.write_line("{SUB().contract()}");
si
