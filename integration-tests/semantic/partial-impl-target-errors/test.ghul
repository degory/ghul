namespace Test is
    trait Sink is consume(); si
    class REAL is si
    class GREETER is si

    partial NoSuchType is
        m() is si
    si

    impl Sink for NoSuchType2 is
        consume() is si
    si

    impl REAL for GREETER is
        m() is si
    si
si
