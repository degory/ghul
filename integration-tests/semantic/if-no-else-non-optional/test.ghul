namespace Test is
    plain_if(b: bool) -> string =>
        if b then
            "yes";
        fi;

    if_let_plain(a: object) -> string =>
        if let s: string = a then
            s;
        fi;

    let_no_annotation(b: bool) is
        let x =
            if b then
                5;
            fi;
    si

    entry() is
    si
si
