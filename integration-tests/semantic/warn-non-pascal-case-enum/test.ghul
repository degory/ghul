namespace Test is
    use IO.Std;

    enum my_colour is
        RED,
        GREEN,
        BLUE
    si

    class Main is
        entry() static is
            let c = my_colour.RED;
            if c == my_colour.RED then
                Std.write_line("red");
            fi
        si
    si
si
