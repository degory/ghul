namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            let s = "hello";

            s[0] = 'H';

            Std.write_line(s);

            return;
        si
    si
si
