namespace Test is
    trait T is
        flag: bool => false;
    si

    class A: T is
        init() is si
    si

    class B: A is
        flag: bool => true;
        init() is si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
