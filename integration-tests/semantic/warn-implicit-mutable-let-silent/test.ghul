namespace Test is
    use IO.Std;

    class Main is
        double(n: int) -> int static is
            let result = n * 2;
            return result;
        si

        entry() static is
            let x = 21;
            Std.write_line("{double(x)}");
        si
    si
si
