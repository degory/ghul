namespace Test is
    use Std = IO.Std;

    class Main is
        function[T](t: T) -> T static;
        function[T]() -> T static;
    si

si

namespace Other is
    entry() static is
        let b: int = Test.Main.function`[int]();
        let c: (int, int) = Test.Main.function`[(int, int)]((1, 2));

        let x: char = Test.Main.function`[int]();

        return;
    si
si
