namespace Test.GenericInheritance is
    use Collections;

    class Thing[T] is
        init() is si

         // FIXME: Thing's T is getting mixed up with Iterable's T here:
        test_iterable(t: Iterable[Test]);        
    si

    class Test is
        test() is
            let thing_int = new Thing[int]();

            let ti = thing_int.test_iterable([self]);            
        si
    si
si