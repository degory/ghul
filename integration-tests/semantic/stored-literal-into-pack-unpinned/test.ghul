use default
use Ghul.`>>

// Nothing pins the pack the stored literal fills: no later call, no
// expected type. The parameters that could not be typed are named.
double(x: int) -> int => x * 2

let plus = (a, b) => a + b
let composed = plus >> double
