@no_std()
namespace Test is
    class A is
        _helper() -> int => 1;
        // same-class call: allowed
        own() -> int => _helper();
    si

    class B is
        // cross-class call to a private underscore method: rejected
        other(a: A) -> int => a._helper();
    si
si
