namespace Test is
    class Main is
        gen() -> Collections.Iterable[int] static is
            let x = 10;
            yield x;
        si

        entry() static is
            return;
        si
    si
si
