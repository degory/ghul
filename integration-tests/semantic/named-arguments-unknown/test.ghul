namespace Test is
    class Main is
        entry() static is
            f(x = 1, z = 2);

            return;
        si

        f(x: int, y: int) static is
            return;
        si
    si
si
