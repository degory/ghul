namespace Test is








    use Std = IO.Std;

    class Main is
        entry() static is
            

            return;
        si
    si

    class BASE is
        test() is
        si
    si

    class INTERMEDIATE: BASE is
        test() is
            
        si        
    si

    class DERIVED: INTERMEDIATE is
        test() is            
        si
    si

    trait Interface[T] is
        blah();
    si

    class BASE_2 is
        init() is            
        si
        
        blah();
    si

    class INTERMEDIATE_2: BASE_2 abstract is
    si
    
    class DERIVED_2[T]: INTERMEDIATE_2, Interface[T] abstract is
        init() is
            
        si        
    si    
si