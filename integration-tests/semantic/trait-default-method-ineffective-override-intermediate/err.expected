test.ghul: 13,9..13,16: error: override of Test.T.compute(x: int) -> Ghul.int is ineffective: intermediate class Test.A does not redeclare compute from Test.T, so it has no vtable slot to override. Add `compute: ... => super.compute;` (or an explicit body) in A to expose the slot
