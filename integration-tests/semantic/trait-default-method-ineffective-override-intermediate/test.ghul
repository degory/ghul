namespace Test is
    trait T is
        compute(x: int) -> int is
            return x;
        si
    si

    class A: T is
        init() is si
    si

    class B: A is
        compute(x: int) -> int is
            return x + 1;
        si
        init() is si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
