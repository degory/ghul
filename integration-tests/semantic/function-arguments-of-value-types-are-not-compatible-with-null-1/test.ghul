namespace Test.FunctionValueTypeArgumentsAreNotCompatibleWithNull is
    use Collections;

    sum(a: int, b: int) -> int is
        return a + b;    
    si
    
    entry() is
        // OK
        sum(1, 2);

        // expect error
        sum(null, null);
    si
si