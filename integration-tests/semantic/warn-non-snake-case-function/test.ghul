namespace Test is
    use IO.Std;

    class Main is
        entry() static is
            DoSomething();
        si

        DoSomething() static is
            Std.write_line("hi");
        si
    si
si
