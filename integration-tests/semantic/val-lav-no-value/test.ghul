use IO.Std.write_line;

entry() is
    // Last statement is a LET — does not provide a value, so the
    // block cannot be used as the RHS of a typed let initializer.
    let x: int = val write_line("setup"); let unused = 99; lav;
    write_line("x = {x}");
si
