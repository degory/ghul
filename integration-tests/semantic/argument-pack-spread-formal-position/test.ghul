namespace Test is
    // Tests: a formal that absorbs the call's remaining arguments has
    // nothing to leave for the ones after it, so it has to be the last.

    trailing[T.., U](f: T.. -> U, v: T.., extra: int) -> U => f(v);
si
