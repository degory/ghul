namespace Test is
    class FOO(x: int, y: int) is
        _x;
        _y;

        init(.., extra: int, ..) is
            _x = _x + extra;
        si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
