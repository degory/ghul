namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            return;
        si
    si

    trait HasMethodTest is
        test();
    si

    trait HasPropertyTest is
        test: int;
    si

    class HAS_BOTH: HasMethodTest, HasPropertyTest is
        test: int => 42;
    si
si
