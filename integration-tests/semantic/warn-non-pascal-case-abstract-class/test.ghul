namespace Test is
    use IO.Std;

    class my_base abstract is
        describe() -> string;
    si

    class CHILD: my_base is
        init() is si

        describe() -> string => "child";
    si

    class Main is
        entry() static is
            Std.write_line(CHILD().describe());
        si
    si
si
