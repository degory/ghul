namespace Test is
    class Main is
        takes_int(n: int) static is si

        entry() static is
            // A bare `default` in a call-argument position cannot be
            // resolved: the parameter type is not known at the point
            // the argument is checked. `default[int]` works here.
            takes_int(default);
        si
    si
si
