// A tail whose type does not match the declared return type is an
// error at the tail, replacing the definite-return warning.

namespace Test is
    broken() -> int is
        "not an int"
    si
si
