namespace Test is
    use Std = IO.Std;

    class Main is
        test() -> object static => "hello";
    si

    entry() static is
        let s: string = Main.test(); // expect error object is not a subtype of string
    si
si