namespace Test is
    class Main is
        gen() -> Ghul.Pipes.Pipe[bigint] static is
            yield cast long(2.0);
        si

        entry() static is
            return;
        si
    si
si
