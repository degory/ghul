// A name on its own in a class body is the primary-constructor capture
// shorthand. Written in a class with no primary header there is nothing
// for it to capture, and it carries neither a type nor an accessor, so
// it is reported here rather than confusing a later pass.
class TEST is
    member
si
