use Ghul.Pipes
use Collections
use IO.Std.write_line

entry() is
    let xs = [1, 2, 3, 4]
    let ys = [1.0, 2.0]

    write_line("{xs |> reduce(0, int.`+)}")
    write_line("{ys |> reduce(1.0, double.`*)}")

    let named: (int, int) -> int = int.`-

    write_line("{named(3, 1)}")
si
