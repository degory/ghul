namespace Test is
    @Ghul.Internal.CLOSED_ATTRIBUTE()
    class A is
        init() is si
    si

    class B is
        init() is si

        marker: object => typeof Ghul.Internal.UNION_ATTRIBUTE
    si
si
