namespace Test is
    use Std = IO.Std;

    class Main is
        run_ln_runtime() -> bool is
            return run(
                "ln",
                // FIXME: _runtime_path is undefined but no error:
                ["-s", _runtime_path, "."],
                1000,
                null,
                null
            ) != 0;
        si

        run(
            path: string,
            arguments: Collections.Iterable[string],
            timeout: int,
            out_file: string,
            err_file: string
        ) -> int => 0;
    si
si