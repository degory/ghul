// `field` is a member storage class, so it has no meaning at namespace
// scope. The declaration is rejected, and reading it afterwards must
// report nothing further: it stays field-shaped and is declared as a
// variable, rather than becoming a property with no accessors.
namespace G is
    total: int field;

    bump() is
        total = total + 1
    si
si
