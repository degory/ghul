namespace Test is
    use IO.Std;

    enum Colour is
        Red,
        Green,
        Blue
    si

    class Main is
        entry() static is
            let c = Colour.Red;
            if c == Colour.Red then
                Std.write_line("red");
            fi
        si
    si
si
