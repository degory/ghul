namespace Test is



    use IO;

    class Main is
        entry() static is
            

            let f = IO.File.create_text("test.txt");

            Std.error.write_line("hello world");

            IO.File.create_text("test.txt");

            Std.error.write_line("hello world");

            Std.out.write_line();

            Console.error.write_line("should not exist");

            return;
        si
    si
si