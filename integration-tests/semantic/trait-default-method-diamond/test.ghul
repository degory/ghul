namespace Test is
    use Std = IO.Std;

    trait Left is
        ping() is
            Std.write_line("left");
        si
    si

    trait Right is
        ping() is
            Std.write_line("right");
        si
    si

    class BOTH: Left, Right is
        init() is si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
