namespace Test is
    class Main is
        gen[T](xs: Collections.Iterable[T]) -> Collections.Iterable[T] static is
            for x in xs do
                yield x;
            od
        si

        entry() static is
            return;
        si
    si
si
