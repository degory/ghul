namespace TEST.ParseLetUse is
    use Collections;
    use Ghul.Disposable

    class TEST is
        test_simple() is
            let use x = D();

            x = D();
        si
    si

    class D: Disposable is
        init() is si
        
        dispose() is
        si
    si    
si