namespace Test.ParseLetUse is
    use Collections;

    class Test is
        test_simple() is
            let use x = new D();

            x = new D();
        si
    si

    class D: Disposable is
        init() is si
        
        dispose() is
        si
    si    
si