namespace Test is    
    @IL.stub()
    class MultipleSuperClasses: A, Collections.List[int], B, Collections.Iterable[int] is
        
    si
    
    @IL.stub()
    class SuperClassNotFirst: Collections.List[int], A, Collections.Iterable[int] is
        
    si

    class A is
        
    si

    class B is
    
    si    
si