namespace Test is
    class BASE is
        init(x: int) is si
    si

    class CHILD(y: int): BASE is
        _y;

        super(x);
    si

    class Main is
        entry() static is
            return;
        si
    si
si
