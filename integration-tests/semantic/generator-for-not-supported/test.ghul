namespace Test is
    class Main is
        gen(xs: Collections.Iterable[int]) -> Collections.Iterable[int] static is
            for x in xs do
                yield x;
            od
        si

        entry() static is
            return;
        si
    si
si
