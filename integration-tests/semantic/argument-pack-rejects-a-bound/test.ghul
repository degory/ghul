namespace Test is
    // Tests: a pack binds to a tuple, which satisfies no bound, so a
    // bound written on one is rejected where it is written.

    trait Named is
        name: string;
    si

    bad[T..: Named](f: T -> int, v: T) -> int => f(v);
si
