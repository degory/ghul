namespace Test is
    // Tests: `..` on a type parameter is that parameter's bound, so a
    // type bound written alongside it is a second bound and is rejected
    // where it is written.

    trait Named is
        name: string;
    si

    bad[T..: Named](f: T.. -> int, v: T) -> int => f(v);
si
