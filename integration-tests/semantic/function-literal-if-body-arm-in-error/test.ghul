use Ghul.Pipes

entry() is
    let ps = [(x = 3, y = 1), (x = 1, y = 2)]

    let sorted = ps |> sort((a, b) => if a.x != b.x then a.x - nope else a.y - b.y fi)

    let f = (n: int) => if n > 0 then missing(n) else other(n) fi

    IO.Std.write_line("{sorted} {f}")
si
