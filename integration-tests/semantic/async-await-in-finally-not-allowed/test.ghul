namespace Test is
    use Std = IO.Std;

    class Main is
        run() -> Tasks.TASK[int] static is
            try
                return 1;
            finally
                await Tasks.TASK.delay(1);
            yrt
        si

        entry() static is
            Std.write_line("{run().result}");
        si
    si
si
