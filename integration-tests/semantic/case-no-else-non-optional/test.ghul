namespace Test is
    describe(n: int) -> string =>
        case n
        when 1 then "one"
        when 2 then "two"
        esac;

    let_no_annotation(n: int) is
        let x =
            case n
            when 1 then "one"
            esac;
    si

    entry() is
    si
si
