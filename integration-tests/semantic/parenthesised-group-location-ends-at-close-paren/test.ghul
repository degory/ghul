// A parenthesised group ending its line, with the terminator inferred, is
// located from its opening to its closing parenthesis, not on to the next
// statement.
entry() is
    let tuple: string = (1, 2)

    let grouped: string = (1 + 2)

    let r = 3
si
