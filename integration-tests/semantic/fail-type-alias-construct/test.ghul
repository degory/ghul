namespace Test is
    use Collections.MAP

    use StringMap[V] = MAP[string, V]
    use Pair[A, B] = (first: A, second: B)
    use Point = (int, int)

    entry() is
        let wrong_arity = StringMap[int, int]()
        let bare = StringMap()
        let pair = Pair[int, string](1, "x")
        let point = Point(1, 2)
    si
si
