// A generic `T?` formal nested inside a function type is taken
// literally by the CLR: `(int) -> U?` is `Func<int, MAYBE<U>>`, and a
// `Func<int, string?>` is not that delegate for any U. The slot-boundary
// coercion between the optional carriers cannot apply to a nested
// position, so a function value whose type is already fixed - here a
// literal with its return written out - is rejected rather than bound
// with the mismatched delegate passed through as-is. A named function
// reference is the one shape that is not fixed: it has no value yet, so
// it is eta-expanded instead.
namespace Test is
    use Std = IO.Std;

    bind[T, U](value: T?, f: (T) -> U?) -> U? =>
        if value? then f(value) else null fi;

    class Main is
        entry() static is
            let annotated = bind(30, (n: int) -> string? => null);
        si
    si
si
