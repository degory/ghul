// A .NET `const` reads as its value and has no field to write to. What
// refuses the write is the rule that a field is assignable only from
// inside the type that declares it, which a constant in another assembly
// can never satisfy.
entry() is
    int.max_value = 1;
si
