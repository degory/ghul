trait Renderable is
    render() -> string => "default";
si

class Expr: Renderable abstract is
    init() is si

    render() -> string;
si

class NUM: Expr is
    init() is super.init(); si
si

entry() is
    IO.Std.write_line("{NUM()}");
si
