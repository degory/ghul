namespace Test is
    class BOX[T] is
        init() is si
    si

    class Main is
        // A generic named with none of its type arguments is the open
        // generic, which only reflection can hold: every position that
        // needs a type has to say which one.
        show(b: BOX) static is si

        entry() static is
            let v = cast object(BOX[int]())

            if isa BOX(v) then
                return;
            fi

            let a = cast BOX?(v)
            let c = Collections.LIST[BOX]()
            let d: BOX mut

            return;
        si
    si
si
