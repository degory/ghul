namespace Test.Function is
    use Ghul;

    class MAIN is
        init() is
            let x: (int, int, int) mut;
            x = (111,222,333);
            let f: FUNCTION_3[int,int,int,int] mut;
            let t: (int, int, int) -> int mut;
            t = (a: int, b: int, c: int) -> int => a + b + c;
            f = t;
            t = (x: int, y: int, z: int) -> int => x - y -z;
            let u = t(111,222,333);
        si
    si
si

