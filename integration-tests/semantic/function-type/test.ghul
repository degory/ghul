namespace Test.Function is
    use Ghul;

    class Main is
        init() is
            let x: (int, int, int);
            x = (111,222,333);
            let f: FUNCTION_3[int,int,int,int];
            let t: (int, int, int) -> int;
            t = (a: int, b: int, c: int) -> int => a + b + c;
            f = t;
            t = (x: int, y: int, z: int) -> int => x - y -z;
            let u = t(111,222,333);
        si
    si
si

