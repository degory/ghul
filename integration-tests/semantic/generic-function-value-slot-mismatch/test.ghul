namespace Test is
    identity[T](x: T) -> T => x;

    class Main is
        entry() static is
            // The slot pins T = int, but the instantiated signature
            // still does not fit a predicate.
            let predicate: (int) -> bool = identity;
        si
    si
si
