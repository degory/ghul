namespace Test is
    tag(n: int) -> string => "[{n}]";

    run() is
        // A never-optional value type has no absent form to propagate,
        // so ~> over one is an error; |> is the spelling for that.
        let r = 42 ~> tag();
    si
si
