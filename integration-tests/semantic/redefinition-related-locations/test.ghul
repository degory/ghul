namespace Test is
    use Std = IO.Std;

    // expect error: redefining symbol dup_function, related location at the
    // first declaration
    dup_function() -> int => 1;

    dup_function() -> int => 2;

    class DUP_CLASS is
        init() is si
    si

    // expect error: redefining symbol DUP_CLASS
    class DUP_CLASS is
        init() is si
    si

    entry() static is
        return;
    si
si
