namespace Test is
    use Std = IO.Std;

    // expect error: duplicate function, at both declarations, each
    // carrying the other as a related location
    dup_function() -> int => 1;

    dup_function() -> int => 2;

    class DUP_CLASS is
        init() is si
    si

    // expect error: redefining symbol DUP_CLASS, at both declarations,
    // each carrying the other as a related location
    class DUP_CLASS is
        init() is si
    si

    entry() static is
        return;
    si
si
