namespace Test is
    // Wrong arity on the IL.output pragma: the argument-count check in
    // IL generation reports it against the argument list location.
    @IL.output("one.il", "two.il")
    class Main is
        entry() static is
        si
    si
si
