namespace Test is
    class Main is
        gen() -> Ghul.Pipes.Pipe[int] static is
            yield in ["one", "two"];
        si

        entry() static is
            return;
        si
    si
si
