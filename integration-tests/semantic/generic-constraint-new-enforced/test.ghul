// A type argument that does not expose an accessible parameterless
// constructor violates the `new` constraint — both at explicit and
// inferred specialisation.

namespace Test is
    class NoDefault is
        init(x: int) is si
    si

    f[T: new](x: T) -> T => x;

    class Main is
        entry() static is
            f[NoDefault](NoDefault(1));
            f(NoDefault(2));
            return;
        si
    si
si
