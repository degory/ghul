namespace TEST.GenericSelf is
        use Ghul;

    class TEST[T] is
        test_generic_self() is
            let i: OTHER[TEST[T]] = OTHER[TEST[T]](self);
            let j: TEST[T] mut;
            let k: OTHER[TEST[T]] = OTHER[TEST[T]](j);

            k.test(self);
            k.test(j);
        si
    si

    class OTHER[Q] is
        init(q: Q) is
            
        si
        
        test(q: Q) static is

        si
    si    
si
