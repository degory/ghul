namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            return;
        si
    si

    trait HasIntCount is
        count: int;
    si

    trait HasStringCount is
        count: string;
    si

    class HAS_BOTH: HasIntCount, HasStringCount is
        init() is si
    si
si
