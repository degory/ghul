namespace Test is
    class Main is
        entry() static is
            f(a = 1);

            return;
        si

        f(a: int, b: int = default) static is
            return;
        si

        f(a: int, b: string = default, c: int = default) static is
            return;
        si
    si
si
