namespace Test is
    class Main is
        entry() static is
            f(a = 1);

            return;
        si

        f(a: int, b: int) static is
            return;
        si

        f(a: int, b: string, c: int) static is
            return;
        si
    si
si
