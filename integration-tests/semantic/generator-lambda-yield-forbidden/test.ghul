namespace Test is
    class Main is
        gen() -> Collections.Iterable[int] static is
            let bad = () -> Collections.Iterable[int] is
                yield 42;
            si;
            for x in bad() do
                yield x;
            od
        si

        entry() static is
            return;
        si
    si
si
