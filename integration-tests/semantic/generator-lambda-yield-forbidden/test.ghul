namespace Test is
    class Main is
        gen() -> Ghul.Pipes.Pipe[int] static is
            let bad = () -> Ghul.Pipes.Pipe[int] is
                yield 42;
            si;
            for x in bad() do
                yield x;
            od
        si

        entry() static is
            return;
        si
    si
si
