namespace Test is
    use Std = IO.Std;

    class FOO is
        count: int static;

        init(n: int) static is
            count = n;
        si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
