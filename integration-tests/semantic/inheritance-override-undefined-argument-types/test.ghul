namespace Test is    
    class A is
        some_method(arg: undefined_type) is
        si                
    si

    class B is
        some_method(arg: a_different_undefined_type) is
        si
    si    
si