namespace Test is
    class Main is
        bad() static is
            yield 1;
            let x = await Tasks.TASK.from_result(2);
        si

        entry() static is
            return;
        si
    si
si
