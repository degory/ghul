namespace Test is








    use Std = IO.Std;

    class MAIN is
        entry() static is
            

            let f: (int, int) -> int mut;

            f = test();

            return;
        si

        test() -> (int, int) -> int static is
            let capture = "hello";

            let f = (i: int, j: int) -> int is
                let x: int mut;
                x = i;
                let y: char mut;
                y = j;

                let z: int mut;
                z = i + j;

                let c: string mut;
                c = capture;
                let d: long mut;
                d = capture;

                return z;
            si;

            return f;
        si

        test2(i: int, j: int) -> int => i + j;        
    si
si