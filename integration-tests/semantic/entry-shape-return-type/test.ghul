// The name says where the program starts and the shape cannot be
// started at, so the function is the error - and the file's top-level
// statements are not run in its place.
entry(point: int) -> string => "{point}"

IO.Std.write_line(entry(1))
