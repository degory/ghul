let greeting = "hello";
let (a, b) = (1, 2);

greet() is
    IO.Std.write_line(greeting);
si

class HOLDER is
    init() is si

    sum() -> string => "{a}{b}";
si

greet();
IO.Std.write_line(HOLDER().sum());
