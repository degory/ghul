namespace Test is
    use IO.Std;

    class Main is
        entry() static is
            describe(true);
            describe(false);
        si

        describe(b: bool) static is
            case b
            when true then Std.write_line("t");
            when false then Std.write_line("f");
            esac
        si
    si
si
