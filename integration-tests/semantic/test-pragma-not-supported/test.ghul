namespace Test is
    class BOX is
        @test()

        init() is
        si

        describe() -> string is
            @test()

            return "box";
        si
    si

    entry() is
        IO.Std.write_line(BOX().describe());
    si
si
