namespace Test is
    entry() is
        let expression_bodied = (name: string) => null;
        let block_bodied = (name: string) is return null; si;

        let typed: (string) -> string? = name => null;
        IO.Std.write_line("[{typed("a")}]");
    si
si
