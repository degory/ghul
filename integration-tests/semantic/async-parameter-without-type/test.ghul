use default

// An async function whose parameter has no type yet: the missing type is
// reported once, and the state machine carries the parameter at an error
// type rather than failing.
worker(n) -> Tasks.TASK[int] is
    await Tasks.TASK.delay(1)
    return n
si
