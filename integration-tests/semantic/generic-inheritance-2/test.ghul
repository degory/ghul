namespace Test.GenericInheritance is
    use System; 
    use Collections;

    class MAIN is
        check_thing_int(ti: THING[int]);

        test(it: DIRECT_INHERIT_NO_PARAMETERS) is
            check_thing_int(it);
        si

        testDirectInheritNoParameters() is
            let it = DIRECT_INHERIT_NO_PARAMETERS();

            check_thing_int(it);
        si
    si

    class THING[T] is
    si

    class DIRECT_INHERIT_NO_PARAMETERS: THING[int] is
        init() is si

        test() is
        si
    si
si