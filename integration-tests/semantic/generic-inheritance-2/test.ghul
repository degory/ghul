namespace Test.GenericInheritance is
    use System; 
    use Collections;

    class Main is
        check_thing_int(ti: Thing[int]);

        test(it: DirectInheritNoParameters) is
            check_thing_int(it);
        si

        testDirectInheritNoParameters() is
            let it = DirectInheritNoParameters();

            check_thing_int(it);
        si
    si

    class Thing[T] is
    si

    class DirectInheritNoParameters: Thing[int] is
        init() is si

        test() is
        si
    si
si