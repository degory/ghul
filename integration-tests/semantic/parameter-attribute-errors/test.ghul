class NEEDS_INT: System.Attribute is
    init(value: int) is si
si

handler(@Nonexistent() id: int) -> int => id;

mismatched_handler(@NEEDS_INT("not an int") id: int) -> int => id;

entry() static is
    return;
si
