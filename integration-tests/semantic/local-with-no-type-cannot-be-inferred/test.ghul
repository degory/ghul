use default

// A local that nothing gives a type is reported where it is declared.
let missing = null
write_line("absent tests as {missing?}")

unassigned() is
    let v mut
    write_line("{v ?? false}")
si

unassigned()
