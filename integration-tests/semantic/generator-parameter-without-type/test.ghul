use default

// A generator header being typed: the parameter has no type yet. The
// missing type is reported once, and the generator's state machine
// carries the parameter at an error type rather than failing.
worker(n) -> Pipe[int] is
    yield 1
    yield n
si
