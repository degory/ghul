namespace Test is
    trait T is
        flag: bool => false;
    si

    class A: T is
        init() is si
    si

    class MID: A is
        init() is si
    si

    class B: MID is
        flag: bool => true;
        init() is si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
