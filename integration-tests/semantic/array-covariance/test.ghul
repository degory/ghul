namespace Test is
    class Main is
        entry() static is
            

            let o: object[] mut;
            o = [1, 2, 3, 4]; // fail expected: valuetype array not assignable to object

            let s: string[] mut;
            s = [1, 2, 3, 4];

            let b: BASE[] mut;
            b = [BASE(), DERIVED()];
            b = [DERIVED(), DERIVED()];
            
            let d: DERIVED[] mut;
            d = [DERIVED(), DERIVED()];
            d = [BASE(), DERIVED()];

            return;
        si
    si

    class BASE is
        init() is si
    si

    class DERIVED: BASE is
        init() is si
    si

si