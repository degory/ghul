// A guard `if` without `else` as the tail of a non-void function is
// an error: the fall-through path delivers no value. Appending the
// `;` restores the old statement reading (and the definite-return
// warning).

namespace Test is
    broken(c: bool) -> int is
        if c then
            return 1;
        fi
    si
si
