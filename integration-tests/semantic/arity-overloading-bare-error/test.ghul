namespace Test is
    class FOO[T] is
        init() is si
    si

    class FOO[K,V] is
        init() is si
    si

    class Main is
        entry() static is
            // Both arities are generic; bare `FOO` must request brackets.
            let x: FOO;

            return;
        si
    si
si
