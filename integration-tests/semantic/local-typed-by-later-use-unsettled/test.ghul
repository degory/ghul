namespace Test is
    use IO.Std.write_line;

    // Nothing uses the local in a way that says what `_()` builds.
    entry() is
        let nothing = _();

        write_line("{nothing}");
    si
si
