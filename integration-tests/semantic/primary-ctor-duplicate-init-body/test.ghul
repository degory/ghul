namespace Test is
    class FOO(x: int) is
        _x;

        init(..) is
            _x = _x + 1;
        si

        init(..) is
            _x = _x + 2;
        si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
