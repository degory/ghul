use IO.Std.write_line;

entry() is
    let width = "10";

    write_line("[{1234,width}]");
si

other() is
    // an unresolved alignment reports only its own error, not a
    // cascading alignment-type error
    write_line("[{1234,no_such_name}]");
si
