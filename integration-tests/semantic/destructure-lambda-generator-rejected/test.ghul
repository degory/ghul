namespace Test is
    use Ghul.Pipes
    entry() is
        let f = ((a, b)) -> Ghul.Pipes.Pipe[int] is
            yield a;
            yield b;
        si;
        f((1, 2)) |> each(x => IO.Std.write_line("{x}"));
    si
si
