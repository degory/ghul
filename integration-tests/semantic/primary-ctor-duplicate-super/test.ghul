namespace Test is
    class BASE is
        init(x: int) is si
    si

    class CHILD(x: int, y: int): BASE is
        _y;

        super(x);
        super(x);

        do_nothing() is si
    si

    class Main is
        entry() static is
            return;
        si
    si
si
