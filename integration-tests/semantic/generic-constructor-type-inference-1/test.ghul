use IO.Std.write_line;

entry() is
    let t_int_int: THING[int,int] = THING(123,456);

    let t_string_int: THING[string, int] = THING("hello", 456);
si

class THING[T,U] is
    init(t: T, u: U) is si
si

err() is
    let x: int = "err"; // deliberate error so we know we got this far
si
