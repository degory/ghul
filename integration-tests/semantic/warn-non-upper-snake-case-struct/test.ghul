namespace Test is
    use IO.Std;

    struct Point is
        x: int public;
        y: int public;

        init(x: int, y: int) is
            self.x = x;
            self.y = y;
        si
    si

    class Main is
        entry() static is
            let p = Point(3, 4);
            Std.write_line("{p.x},{p.y}");
        si
    si
si
