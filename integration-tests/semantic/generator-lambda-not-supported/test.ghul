namespace Test is
    class Main is
        gen() -> Collections.Iterable[int] static is
            let f = () => 42;
            yield f();
        si

        entry() static is
            return;
        si
    si
si
