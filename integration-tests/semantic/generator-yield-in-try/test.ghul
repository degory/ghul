
namespace Test is
    use Std = IO.Std;

    class Main is
        with_try() -> Collections.Iterable[int] static is
            try
                yield 1;
            catch ex: System.Exception
                yield 2;
            yrt
        si

        entry() static is
            return;
        si
    si
si
