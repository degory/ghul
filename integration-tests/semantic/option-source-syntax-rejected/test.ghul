namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            // `OPTION[T]` is no longer a user-spellable type. The
            // value-type representation of `T?` (= System.Nullable<T>)
            // is internal — users write `T?`. The unresolved-name
            // diagnostic confirms the type_name_map entry is gone.
            let x: OPTION[int] = 42;
        si
    si
si
