namespace Example is
    enum Color is
        RED,
        GREEN,
        BLUE
    si
si

namespace Main is
    use Example.Color.RED;
    use Example.Color.GREEN;

    use Example.Color.BLUE;
    entry() static is
        let c mut = RED;
        if c == GREEN then
            c = BLUE;
        fi
        return;
    si
si

