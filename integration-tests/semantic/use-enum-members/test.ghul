namespace Example is
    enum Color is
        Red,
        Green,
        Blue
    si
si

namespace Main is
    use Example.Color.Red;
    use Example.Color.Green;

    use Example.Color.Blue;
    entry() static is
        let c = Red;
        if c == Green then
            c = Blue;
        fi
        return;
    si
si

