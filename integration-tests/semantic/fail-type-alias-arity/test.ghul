namespace Test is
    use Pair[A, B] = (first: A, second: B);
    use Names = string[];

    too_few(p: Pair[int]) is si
    too_many(p: Pair[int, int, int]) is si
    not_generic(n: Names[int]) is si
si
