namespace Test is
    use IO.Std;

    union Color is
        RED;
        GREEN;
        BLUE;
    si

    class Main is
        entry() static is
            describe(Color.RED());
        si

        @suppress("non-exhaustive-case")
        describe(c: Color) static is
            case c
            when _: Color.RED then Std.write_line("r");
            when _: Color.GREEN then Std.write_line("g");
            esac
        si
    si
si
