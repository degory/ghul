namespace Test is













    use Std = IO.Std;

    class Main is
        entry() static is
            

            let s = STABLE();

            s.do_stuff(10, "twenty");

            let d = DERIVED();

            d.do_other_stuff(20, "forty");

            return;
        si
    si
si