namespace Test is

    // FIXME this is not relevant anymore
    
    @KEEP()
    class Stable is
        init() is si
        do_stuff(a: int, b: string) -> string => "a is: " + a + " b is: " + b;
    si

    @KEEP()
    class Derived: Stable is
        init() is si
        do_other_stuff(x: int, y: string) -> string => "x is: " + x + " y is: " + y;
    si
si
