// A constant reached through a name brought into scope by `use` is
// written to by a different path from the member-access form, which is
// turned away as a field only its declaring type may assign to. Neither
// spelling has a field to write to, so both have to report.
use int.max_value;

entry() is
    max_value = 1;
si
