@no_std()
namespace Test is
    class A(v: int private) is
        // same-class read of the captured member: allowed
        own() -> int => _v;
    si

    class B is
        // cross-class read of a private capture: rejected
        other(a: A) -> int => a._v;
    si
si
