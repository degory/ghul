// impl and partial cannot reopen a type imported from another assembly
namespace App is
    trait PRINTER is
        print() -> string;
    si

    impl PRINTER for System.Text.StringBuilder is
        print() -> string => "sb";
    si

    partial System.Text.StringBuilder is
        described() -> string => "sb";
    si
si
