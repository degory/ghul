// An asynchronous entry returns its task, which is neither int nor
// void, so it is ruled out by its return type like any other.
entry() -> Tasks.TASK is
    await Tasks.TASK.yield()
si

IO.Std.write_line("started")
