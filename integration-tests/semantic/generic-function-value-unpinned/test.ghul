namespace Test is
    identity[T](x: T) -> T => x;

    class Main is
        entry() static is
            // Nothing here says what to instantiate identity at, so
            // the reference has no value form to load.
            let unpinned = identity;
        si
    si
si
