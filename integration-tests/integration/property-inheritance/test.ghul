namespace Test is
    use Std = IO.Std;

    class Main is
        entry() static is
            return;
        si
    si

    class Base is
        property: int;
    si

    class DERIVED: Base is
        property: int => 456;

        init(i: int) is
            // FIXME: property can be assigned by casting to Base, so is there any point in this error?
            property = 123;
        si
    si    
si